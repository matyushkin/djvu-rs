# Experiments Index



Navigation map for `PERF_EXPERIMENTS.md`. Read this first; open the full entry only when you need numbers or code. Updated: 2026-07-12.

**Maintenance rule:** every `###` entry appended to `PERF_EXPERIMENTS.md` gets a row here in the same PR. A 2026-07-10 audit found ~75 entries missing (the 2026-06-09..19 compositor sweep, the 2026-07-01/02 perf-swarm + parallelism sweeps, round 56, and everything before 2026-05-16); four freshly-filed experiment issues (#587, #574, #560, #564) had to be closed as already-answered by those unindexed entries. An incomplete index actively causes duplicate work.

## Legend

Status: **K** = Kept · **R** = Reverted · **X** = Rejected · **D** = Diagnostic/infra · **F** = Needs follow-up

---

## Experiment Table

| ID | Date | Component | Status | Effect | Notes / Related |
|----|------|-----------|--------|--------|-----------------|
| WASM_DJVUJS_COMPARE (#596) | 2026-07-12 | wasm/browser benchmark | **D/K** | Headless Chrome @100 dpi: wasm scalar beats djvu.js **6.4-23.0x** first-page and **6.4-15.3x** selected-page; simd128 beats djvu.js **6.8-23.0x** first-page and **6.7-15.3x** selected-page; large photo page 6.6 s djvu.js vs 0.85 s scalar / 0.62 s simd | New `scripts/bench_wasm_djvujs.mjs` CDP harness builds/runs wasm scalar + simd128 against `djvujs-dist` 0.5.4 (repo vendors only shared assets, not runtime bundle). Published table in `BENCHMARKS_RESULTS.md`; raw local JSON in ignored `target/bench-djvujs/issue-596.json`. Diagnostic kept; no perf follow-up from the comparison.  Round-109 |
| WASM_SIMD128_SHIP (#581) | 2026-07-13 | wasm packaging/runtime selection | **K** | Dual browser package ships one JS entry plus scalar and `simd128` wasm artifacts; runtime `WebAssembly.validate()` selects SIMD when supported. Fresh render direct run **2.609→2.454 ms (-5.9%)**; 5-repeat median fresh render **-16.0%**, progressive **-10.3%**, cached-page neutral/noisy **+2.6%** median. | `make wasm` wraps `scripts/build_wasm_dual.sh`; docs updated for npm layout and `selectedWasmVariant()`. Scalar fallback preserved; not claimed as an across-the-board win. Round-108 |
| DIFF_FUZZ_BUCKETS (#577) | 2026-07-13 | interop (diff-fuzz divergence triage) | **Fixed** (partial) + **K** (documented) | `our-renders-what-they-reject` 221→**196** (−25) on the round-45/46 seed-42/2100-mutant sweep, all Δ from strict-mode BG44; `both-render-fail` 117→142; every other class bit-identical, 0 new crashes | `diff_fuzz` now buckets by (mutated chunk, ddjvu error class) + pixel evidence. Root cause (top-3 buckets + FG44): `PageLayers::bg44`/`decode_fg44` swallowed a detected `decode_chunk` error in *strict* mode (kept a partial image / reported "no foreground") — now propagates; permissive path decoupled to keep its own graceful-degradation loop. Supersedes round-46's "kept, benign" call on the BG44 serial-mismatch bucket (now mirrors DjVuLibre's reject). Residual: most BG44 "Unexpected End Of File" mutants still diverge — the ZP decoder tolerates truncation *without* raising an error at all (round-45 finding 2, still open, decoder-internals scope). Round-111 |
| X86_V3_FEATURE_BISECT (#566) | 2026-07-13 | ZP decode (x86 Callgrind feature bisect) | **X** | ubuntu Callgrind Ir vs default: JB2/ZP best case **bmi2 −1.91%** / **bmi1+bmi2 −2.02%** / **x86-64-v3 −1.71%** — nowhere near the claimed −4..−28% ZP win; BMI1 alone **+2.12%** (regression). IW44 sees v3 **−7.46%** Ir but that is outside the ZP-scoped keep gate. | Infra kept (`scripts/x86_feature_callgrind.sh` + `workflow_dispatch` job). No RUSTFLAGS / target-cpu shipping for ZP. Round-110 |
| QUALITY_METRIC_GATE (#585) | 2026-07-13 | quality metrics (lossy gates) | **X** | Clean-room std-only `f64` MS-SSIM prototype did **not** strictly reduce inversions vs OCR ground truth: OCR grid pairwise inversions stayed **0 vs 0** (SSIM vs MS-SSIM) on unequal-agreement pairs; MS-SSIM was more compressed near 1.0 and did not catch OCR cliffs earlier. | Prototype/harness columns reverted; no `src/quality.rs` API or harness column adopted. OCR grid: text 100% holds through 4%, breaks at 6%; scan 100% holds through 8%, breaks at 10%. Colour/mask harness attempts on this checkout hit pre-existing `Iw44(Invalid)` render failures, so they supplied no adoption evidence. Round-107 |
| OCR_WORD_DUP (#602) | 2026-07-13 | OCR (JB2 word memoization) | **X** | unique-word 98.9%/80.9% on pathogenic/watchmaker → bound 1.01×/1.24× ≪ 5× keep bar | CHEAP probe only (`ocr_word_dup_probe`); no OCR. Round-104 |
| IW44_FWD_TRANSFORM (#578) | 2026-07-13 | IW44 encoder (forward DWT diagnostic) | **X** | worst colorbook pages 61/2/59/1/60: ours-c44 gaps **478–571 B**, but production-vs-DjVuLibre forward DWT deltas are **0 changed coeffs** across every Y/Cb/Cr plane and band 0; transform explains **0 B / 0.0%** of the gap | Added `iw44-probe` coefficient-diff diagnostic plus `examples/iw44_forward_transform_probe.rs`; fetched DjVuLibre scalar schedule and compared on identical encoder input planes. Rejects the unvalidated IW44_ENTROPY_PROBE root-cause hypothesis and closes the band-0/DC size-gap thread as entropy-coding-complete for zero-PSNR levers. Round-106 |
| PDF_JBIG2_GENERIC (#568) | 2026-07-13 | PDF export (JBIG2 mask encoder) | **X** | Minimal MQ-coded generic-region JBIG2 beats G4 in bytes but fails the required ≤0.5× G4 gate: TOTAL 42,042,364 / 61,652,286 = 0.682; watchmaker 0.646, cable 0.726, pathogenic 0.683 | Encoder prototype implemented, measured, then reverted. Future attempt needs symbol dictionary + text region, not page-sized generic regions. Round-105 |
| CORPUS_DIVERSITY (#558) | 2026-07-12 | bench/corpus (tier-2 fixtures) | **K** (infra) | newspaper/mixed/map/CJK/Cyrillic/photo fixtures checked in (~3.3 MB); interop green; baseline table + `tier2_corpus_render` benches; chroma-half ask superseded by #561 → photo page instead | `tests/corpus/README.md` tier-2 table; `corpus_tier2_baseline`; fetch SHA-256 for whole-docs. Round-103 |
| FG44_FOREGROUND (#591) | 2026-07-12 | encode (foreground model) | **R** | FG44 loses to FGbz on fidelity AND bytes everywhere: gradient text dE 8.3 vs 2.1, stamp 9.5 vs 4.9 (worse than one flat colour), all-masked gradient 18.5 vs 7.4; finer reduction worse (rules out coarseness); ddjvu decodes the same wrong values (dark-ink green crush) | Full encode path implemented + ddjvu-interop-verified (single FG44 chunk required — 'Duplicate foreground color info' otherwise), then reverted. FGbz Exact is near-lossless per blit; foreground ink varies *between* components, not within. Probe examples/fg44_probe.rs. Round-101 |
| METADATA_PLANES (#597) | 2026-07-12 | fuzz/interop (TXTz/NAVM/ANT) | **K** (infra) | djvused differential over mutated corpus: **0 content-diverge** on all three planes; every divergence adjudicated benign (our INFO-version gate vs djvused not validating INFO; our lenient DIRM/NAVM #553 vs djvused whole-file reject) | New `fuzz_metadata` target (CI weekly + oss-fuzz) mirrors `tests/metadata_roundtrip.rs` soaks (800 LCG cases); `diff_fuzz` gains `--djvused` metadata classes with per-(page,plane) baseline gating; `navm_fgbz.djvu` joins default files (NAVM+ANTz). Local libFuzzer still blocked (round-64 dyld, re-confirmed) → soaks+CI substitute. Round-102 |
| QUALITY_AUTO (#570) | 2026-07-12 | encode (profile auto-detection) | **K** | corpus: boy→Photo, bilevel→Lossless, colorbook/navm→Quality; photo→bilevel misroute provably blocked; overhead 0.184 ms (≈1% самого быстрого энкода) | `classify_content`: chroma + luma-histogram (bins/modes/mass) + sharp-edge density (photos 0.04% vs text ≥0.36%); CLI `--quality auto` (dir: bundle-wide). Column stride must stay 1 (stride-2 ложные края). Boundary: пере-рендеренный watchmaker@150 честно двухтоновый → Lossless. Round-82 |
| INCR_SAVE (#595) | 2026-07-12 | mutation (bundled save cost) | **K** | 26.6 MB bundle, same-size INFO edit: **1 B written** (2.7·10⁷× fewer bytes) vs full rewrite, wall 1.8×; size-changing edit: no byte win (DIRM/NAVM at head shift everything) — hit-rate is binary; djvudump+ddjvu clean | New `save_patched(&mut File)`: emit in memory, diff vs retained original, write changed span only (prefix always skipped, suffix too when length unchanged), truncate/extend; `PatchTargetMismatch` fails closed. Tests: clean/same-size/size-changing/wrong-target. Probe `examples/incremental_save_probe.rs`. Drive-by → #657 (merge output vs DjVuLibre). Round-91 |
| MERGE_DIRM_INTEROP (#657) | 2026-07-12 | interop (merge/split DIRM) | **Fixed** | 504-component merge: djvudump/ddjvu errored ("no indirect entries allowed") → both clean; split likewise; lazy loader now indexes merged bundles with 0 probes (#584 synergy) | Three writer defects in `djvm.rs`: zeroed offset table (DjVuLibre's indirect-entry marker), DIRM version byte 0x80 (version-0 layout differs; real files write **0x81**), zeroed size table. Fix: two-pass `partial_emit_with_offsets` (the `encode_djvm_bundle_jb2` pattern), `build_bundled(sizes)`, version 1 everywhere. Regression test on version/offsets/sizes. Round-92 |
| PGO_ARTIFACTS (#586) | 2026-07-12 | dist (PGO release binaries) | **R** | CLI-level A/B (hyperfine): held-out renders ±1–2% noise; whole-doc PDF export **697→730 ms = PGO 5% slower** (untrained export paths de-prioritized); round-5's −15% was in-process cold render only | Decision-rule gate 1 (≥8% held-out) missed by an order of magnitude → x86 второй платформы и BOLT не достигнуты (BOLT — moot at artifact level). `make pgo` остаётся локальным opt-in для render-heavy встраиваний. Round-95 |
| OCR_INPUT_AB (#603) | 2026-07-12 | OCR (input selection) | **K** (diagnostic) + **Fixed** (dpi) | bilevel: A=B=C (98.1/98.4% char); halftone cable: Sauvola re-binarization **−7 п.п.** char; colour colorbook: JB2-mask-only **52.0% vs 74.3%** (misses colour text); no variant dominates → default (raw render) stands | Harness `examples/ocr_input_ab.rs` (3-way, embedded text layer as reference, OCR_QA metrics). Drive-by fix: `cmd_ocr` hard-coded `dpi: 300` — now per-page `page.dpi()` (Tesseract layout is dpi-sensitive; 400/600-dpi scans were mis-scaled). Round-93 |
| BLOCK_CLASSIFY (#562) | 2026-07-12 | segment (mixed layouts, opt-in) | **K** (opt-in) | halftone mixed page: Sjbz **−35%** and vs-true-photo ΔE 38.6→**7.2** / ssim 0.018→**0.659** (descreening); continuous-tone: photo ink −86…99%, Sjbz −5…10%; pure text **bit-identical** (3 corpus checks) | `SegmentOptions::block_classify`: 32×32 blocks, continuous-tone (non-white >60% ∧ sharp <1.5%) OR halftone (mask flip-density >25% of pairs) → cleared from mask; 3×3 majority smoothing. Probe `examples/block_classify_probe.rs` (synthetic mixed pages incl. Bayer halftone); real fixtures → #558. Round-97 |
| FLATTEN_BG (#593) | 2026-07-12 | segment (flat-field BG, hypothesis) | **R** | real scans: ±0.0% BG44 (uniform fields, gain≈1); synthetic stained scan: paper whitens 193→255 but BG44 **+12.3%** with both field implementations (per-pixel box mean AND smooth bilinear grid) | Mechanism inverted: smooth illumination is low-frequency = nearly free in IW44 (whole stained BG = 2.5 KB), while >1 gain amplifies paper *grain* — the expensive part. Code reverted; recommendation on the issue: viewer/export-side white-balance (0 encoded bytes). Round-98 |
| STRIPWISE_IW44 (#594) | 2026-07-12 | export memory (gigapixel IW44) | **R** | standard DjVu (subsampled BG): PDF export **flat 25 MB** across 2× area — already O(row); full-res IW44 (Photo profile): peak 15.28 B/px but **coeff floor 5.73 B/px** (slice-ordered ZP, irreducible) is > half the 9.1 B/px streaming peak → strip-wise ceiling **1.5×**, below the ≥2× bar | Measured (ru_maxrss, native + 2× export) that streaming exporters already bound standard-page memory; the issue's 12 B/px model applies only to full-res IW44 where the coefficient floor dominates. No IDWT rewrite. Round-100 |
| LOSSY_PRESET_CAL (#572) | 2026-07-12 | JB2 lossy presets (OCR-derived) | **K** | scan class: `lossy_scan` t 0.02→**0.06** (−0.70% vs −0.10% Sjbz at equal 100% min-agreement over 4 pages; 100% holds through 8%, breaks at 10%); text class: shipped 0.02 **validated as the derived optimum** (100% only through 4%, breaks at 6%) | Grid harness `examples/ocr_preset_grid.rs` (threshold×despeckle, min per-page char agreement vs lossless mask); derivation = highest fully-agreeing point − 1 step. Methodology note: 2-page pilot suggested 6% for text — overturned by 4 pages; min-agreement needs ≥4 pages. Round-99 |
| DESKEW (#592) | 2026-07-12 | encode (skew correction, opt-in) | **K** (opt-in) | skew costs JB2 dearly: 1° = **+36/+150/+384%** Sjbz (0.3° = +20/+102/+241%); OCR insensitive (≥99.2%); corrector recovers **70–96%** of the loss at ≥1° (watchmaker/DjVu3Spec), weak pocket at ~0.5° (±0.02° estimator bias; 0.02° residual costs more than it recovers) and on halftones | `SegmentOptions::deskew`: projection-profile sharpness maximization (±5° coarse-to-fine + parabolic peak), bilinear upright rotation before binarization at ≥0.15°. Fixed two estimator defects (row-stride comb → always 0°; plateau tie-break). Probe `examples/deskew_probe.rs` + synthetic unit tests. Round-96 |
| MULTI_INCL (#624) | 2026-07-12 | decode (INCL resolution) | **Fixed** | czech.djvu: every mask was `MissingSharedDict` → page 1 mask 1095×1750 **byte-identical to ddjvu** `-mode=mask`; bundled fixtures unaffected | Root cause ≠ the issue's "missing external files": pages carry 3 INCLs (shared_anno ANTz-only first) and both parser+lazy loader resolved only the first. Fix: scan all INCLs, take the first with a Djbz; lazy loader skips non-Djbz includes. Sync+lazy regression tests. Round-94 |
| ALLOC_PROFILE (#600) | 2026-07-12 | infra (allocation profiling) | **K** (infra) + **R** (fix attempt) | baseline map: encode 786.6 MB total / t-gmax 514 MB (top: 193.6 MB `extract_ccs` grids), PDF export 180 MB, thumbnails 47 MB, warm render = 10× output pixmaps; scratch-reuse fix for the top site: ~−1.5% (interleaved A/B), below the 3% bar → reverted | New dev-only `alloc-profile` feature + `examples/alloc_profile.rs` (dhat global allocator, 5 scenarios, one per process, JSON for the DHAT viewer). Follow-up candidates recorded: thumbnail-path `extract_mask`, `PlaneDecoder` full-plane alloc on sub-decodes. Round-89 |
| GENERATION_LOSS (#601) | 2026-07-12 | encode robustness (re-encode cycles) | **K** (diagnostic) | text pages converge (watchmaker: gen1 ΔE 0.147, fixed point by gen-2, Sjbz frozen); picture page diverges (colorbook: ΔE vs gen0 8.4→**86.6** over 5 gens, vs-prev growing 8→30); cause: mask +68% px over 5 gens — Sauvola re-binarization eats continuous tone each cycle | Harness `examples/generation_loss.rs` (ΔE/ssim vs gen0+prev, bytes, Sjbz proxy) + permanent test `lossless_bilevel_reencode_is_idempotent` (bit-identical mask, gen-2 byte fixed point). Guidance: lossless safe; Quality/Archival safe on text, must not round-trip photos. Follow-up: mask-reuse re-encode API + #562 classifier. Round-90 |
| CLI_TH44 (#590) | 2026-07-12 | CLI encode (TH44 thumbnails) | **K** (opt-in flag) | 12-page bundles: text +33.1% size for **15.0×** faster 128px grid (98.4→6.6 ms); colour +7.3% for **44.3×** (205.0→4.6 ms); ddjvu-clean | `djvu encode --thumbnails` routes to the existing `encode_djvm_layered_shared_with_thumbnails`; warnings on single-page/lossless paths. Default NOT flipped: overhead 7–33% ≫ the <1% bar (TH44 ≈ 2.3–3.6 KB/page). Probe: `examples/th44_grid_probe.rs`. Round-87 |
| EXPORT_COLD_CLONE (#629) | 2026-07-12 | exports (memory, PDF/EPUB/CBZ/TIFF) | **K** | 504-page PDF export peak RSS: seq **2.243 GB → 44.7 MB (50×)**, par **2.336 GB → 240.7 MB (9.7×)**; wall-clock noise; PDF/CBZ byte-identical, EPUB identical modulo `dcterms:modified` | Exporters render each page on a **cold `DjVuPage` clone** — `Clone` already drops the render cache by design, so per-page decode caches die with the clone instead of accumulating O(pages) on the `&DjVuDocument`. Shared-dict decode stays in the `shared_djbz` Arc (no re-decode). Parallel residual = O(chunk) rendered bodies (#606 design). Round-88 |
| PHOTO_PROFILE (#571) | 2026-07-12 | encode (mask-less DjVuPhoto) | **K** | photo: dE 9.34→**2.29** (4× точнее) vs Quality; grayscale: ssim_y 0.973→**0.992**; `ddjvu` декодирует оба | `EncodeQuality::Photo`: INFO+BG44 без сегментации/Sjbz/FGbz; чистый grayscale → `encode_iw44_gray` (одна luma-плоскость); CLI `--quality photo`; размер управляется bpp-бюджетом (`Iw44Target::Bpp`). Бандл-путь остаётся layered (→ #570). Round-81 |
| ADAPTIVE_BG_SUB (#569) | 2026-07-12 | encode (BG44 subsample, opt-in) | **K** (opt-in) | auto = лучший из фиксов на каждой странице: плоские → 12 (fix6 там +78…135% байт за *худший* SSIM), детализированные → 6 (dE лучше) | `SegmentOptions::adaptive_bg_subsample`: доля 12×12-ячеек с luma-разбросом >24 среди немаскированных пикселей → {ceiling,6,3}; под-чернильные пиксели исключены (тест). Default false = байт-идентично. Дефолты профилей — после #558/#570. Round-83 |
| PY_REGION (#583) | 2026-07-12 | djvu-py (region/progressive bindings) | **K** | viewer-pan через регионы **64×** быстрее полностраничного (19.8 vs 1286.6 ms ×8 шагов); 4 потока → ещё 2.1× (GIL отпущен); pytest 44/44 | Новые публичные `Page::{render_region (tile-cache), render_coarse, render_progressive, bg44_chunk_count}` в djvu_rs + зеркала в djvu-py (`py.detach`). Прогрессив байт-идентичен full только на нативном размере (на даунскейле — другой законный путь ресемплинга, зафиксировано). Round-84 |
| PAR_OCR (#573) | 2026-07-12 | CLI OCR (page fan-out, `parallel`) | **K** | watchmaker ×12: 38.7 s → 20.1 s (2 потока, **1.93×**) → 11.3 s (4, **3.42×**) → 8.5 s (8, 4.55×); вывод байт-идентичен; ≈+150–190 MB/воркер (Tesseract) | Rayon fan-out в `cmd_ocr`, бэкенд на задачу (recognize строит свежий Tesseract на вызов — трейт не меняли); внедрение слоёв последовательное по порядку страниц. Drive-by: чинён давний clippy `items_after_test_module` в ocr_tesseract.rs. Round-79 |
| WASM_LAZY_OPEN (#588) | 2026-07-12 | wasm binding (lazy Range open) | **K** (opt-in `wasm-lazy`) | Chrome @12.5 MiB/s, 25.3 MiB / 520-page book: full download+open+render p0 3.9 s → lazy 0.20–0.34 s (**11.5–19×**); open = 1 GET / 64 KiB (0.25% of file); page 50/200: 3/2 GETs; progressive coarse paint 47 ms → full 77 ms; pixels identical; `from_bytes` untouched | `WasmLazyDocument` over new `JsRangeReader` (`AsyncRead+AsyncSeek` from a JS `(offset,len)→Promise<Uint8Array>` callback, 64 KiB-block LRU, JsFuture polled in `poll_read`) driving `from_async_reader_lazy_local` + the #584 DIRM size-table index. Feature `wasm-lazy` = wasm+async+wasm-bindgen-futures; check.sh/CI wasm32 gates extended. Bench: `examples/wasm/bench_lazy_open.html` + throttled `serve_lazy_bench.py`. Round-86 |
| HTTP_RANGE_TTFP (#584) | 2026-07-12 | async lazy open (HTTP Range) | **K** + **Fixed** | 25.3 MiB / 517-component book @12.5 MiB/s: open 321 GETs / 20.5 MiB (79% of file) → **1 GET / 64 KiB**; TTFP 2.05 s → 0.02 s (**103×**, ≥5× bar); page 50/200: 2–3 GETs; 4 MiB/s: 209×; render byte-identical to full open | Real defect in `index_bundled_djvm`: one `FORM`-header probe (seek + 4-byte read) per component. Fix: trust the DIRM 24-bit size table when populated (`DirmComponent.size`), probe only zeroed tables (our writer). Corpus-wide equivalence test `dirm_size_table_matches_form_boundaries`; throttled local HTTP/1.1 Range probe `examples/async_http_first_page.rs` (64 KiB block LRU adapter). Round-85 |
| WASM_SIZE_DIET (#582) | 2026-07-12 | wasm artifact size | **X** (frontier) | best −10.4% (`opt-level=z`, 414.6→371.6 KB) at **~2.8× slower** render (56.6→158.9 ms); `wasm-opt -Oz` after fat LTO: <0.5 KB | Adoption bar (≥20% size at ≤3% speed) nowhere near met; fat-LTO release is already compact and encoders are DCE'd from the viewer surface. Frontier documented in examples/wasm/README.md. Round-80 |
| IW44_CHECKPOINT (#608) | 2026-07-12 | IW44 decode (checkpoint resume) | **K** | repeated full decode **−16…−19%** on 4 multi-chunk fixtures (byte-identical); integrated full render after thumbnail −6…−12%; clone 0.04–0.52 ms; **zero new retention** | `PageLayers::bg44` peeks the existing `bg44_partial` (first-chunk) cache and resumes from a clone — thumbnail→full-view reuses paid work; cold decodes unchanged; no budget/policy change. Retained-across-downgrade checkpoint tier declined per C5_COMPRESS (coeff planes = pixmap size class). Probe: `examples/iw44_checkpoint_probe.rs`. Round-77 |
| TILE_LRU (#576) | 2026-07-12 | render (viewer tile cache) | **K** (LRU) | back-and-forth pan hit rate FIFO 69.4% → **LRU 76.8%** (misses −24%); scans unchanged; budget sweep: 4 MiB thrashes (0%), 8 MiB kept, 16 MiB +7 п.п. for 2× memory | `get_tile` moves hits to the back of the order deque (≤~32 keys); test-only hit/miss/eviction telemetry; scenario test with a ≥30% floor. Retina-class viewports exceed the 8 MiB budget before policy matters — viewport-scaled budget noted as the real follow-up. Round-78 |
| RESOURCE_CEILING_AUDIT (#589) | 2026-07-11 | decode robustness (all codecs) | **Fixed** + **D** | inventory table (SECURITY.md) + closed the one unbounded axis: TXTz `children_count` (i24→~1.5 GB from 3 bytes) reservation cap + `MAX_ZONE_DEPTH=64` recursion guard | Every untrusted-length allocation/loop classified; all bounded except the TXTz zone tree. Reservation now `min(count, remaining/17)`, loop still fails `ZoneTruncated` as before; depth capped like NAVM. 3 regression seeds; real text layers unaffected; SECURITY.md documents the full at-most-X-memory table. Round-75 |
| WASM_BATCH (#610) | 2026-07-12 | wasm binding (multi-page render) | **K** | 8-page batch @4 workers: **4.28×** vs single-page JS loop (94→22 ms); 2 workers 2.31×; no-pool fallback 1.16× (sequential, works); pixels identical, order preserved | Additive `render_pages_batch` → `Vec<WasmPixmap>`: coarse one-page rayon tasks on the opt-in `wasm-threads` pool — the WASM_THREADS revisit-condition fulfilled (fine-grained stays a dead end). Bench: `examples/wasm/bench_batch.html` + COOP/COEP server. Round-76 |
| TIFF_G4 (#579) | 2026-07-11 | TIFF export (bilevel compression, opt-in) | **K** (opt-in) | **2.5–2.8×** smaller than the Deflate default (watchmaker 1.81→0.72 MB); Pillow/libtiff pixel-identical, `tiffinfo`-clean | `TiffOptions::bilevel_compression = G4`: 1-bit CCITT T.6 strips via `smmr::encode_g4` through a minimal in-crate IFD writer (the `tiff` crate has no CCITT encoder); masks via shared-dict-aware `extract_mask`; own-decoder round-trip + IFD-chain tests. Round-73 |
| EPUB_ADAPTIVE_RASTER (#580) | 2026-07-11 | EPUB export (page image format) | **K** (both levers) | adaptive JPEG: colorbook **−76.0%** (36.2→8.7 MB) at ssim_y 0.9969; Gray8 default: cable **−61.9%**, watchmaker **−60.7%**, pixel-identical (Pillow) | `EpubOptions{jpeg_quality, adaptive}` (PDF_ADAPTIVE_RASTER pattern; `epub` feature owns `jpeg-encoder`, #509); Gray8 PNG when the render is pure grayscale; per-page ext/media-type threaded through OPF. watchmaker adaptive = PNG byte-for-byte (guard works). Round-74 |
| BUNDLE_TWO_PASS (#565) | 2026-07-11 | multi-page layered encode (memory) | **K** (bound recorded) | 96-page archival encode: peak RSS 922.9→873.2 MB, wall 3.80→3.05 s, output byte-identical | Pass 1 segments + encodes BG44/TH44 and drops the background pixmap; clustering over borrowed masks (`cluster_shared_symbols_from_refs`, no clones); pass 2 Sjbz/FGbz/assembly. Remaining peak = caller-held `&[Pixmap]` inputs (API bound — the issue's ≥2× needs a streaming-input API); assembly buffer = #606-style writer follow-up. Round-71 |
| TRUE_MRC (#563) | 2026-07-11 | PDF export (opt-in `PdfOptions::mrc`) | **K** (opt-in) | watchmaker **−64.5%** size AND dE 7.38→2.15 / ssim_y 0.794→0.940; irish −86.7% (dE→0.003); navm −78.9%; colorbook falls back (multi-colour FG44) | Background-only `/Im0` at native BG44 resolution when stencils fully cover the foreground (#559/#620 machinery); default path byte-identical; raster policy shared via `encode_img0_body`. Probe: `examples/pdf_mrc_probe.rs`. Defaulting decision deferred to corpus diversity (#558). Round-72 |
| METADATA_CACHE (#605) | 2026-07-11 | page metadata (TXTz/ANTz access) | **K** | warm repeated `text()` 177.1 µs → **3.90 µs (≈45×)**; cold +1.1% (noise); errors uncached | Two `OnceLock<Arc<…>>` slots in `PageLayers` (budgeted via `cached_bytes`, evicted with the page cache); owned APIs clone from cache, new `text_layer_shared`/`annotations_shared` hand out the `Arc`. New `text_extraction_cold` bench splits cold/warm. Round-70 |
| RGB_PNG_EXPORT (#599) | 2026-07-11 | EPUB/CBZ export (payload size) | **K** (lever 1) | EPUB/CBZ **−8.0%** (navm_fgbz), CBZ −9.9% (boy); pixels identical after decode (PIL-verified); wall-clock +2.4% (noise) | Alpha stripped at PNG emit (`ColorType::Rgb`) — pages are always opaque (ALPHA_INL). Lever 2 (RGB-native row mode) declined: emit-time conversion measures at noise, matching ALPHA_INL/ZEROED history; revisit with #557 instruction counts. Round-69 |
| PAR_CBZ (#598) | 2026-07-11 | CBZ export (`cbz` feature, `parallel`) | **K** | **−73.6%** (299.6→81.9 ms, ≈3.7×) on new `export/cbz` bench (watchmaker, 12 pp) | New `src/cbz.rs` lib module (`djvu_to_cbz`/`write_pages`), CLI delegates; render+PNG-encode fan out via rayon, ZIP writes stay serial in index order (EPUB/PDF #298 pattern). Byte-identical to the pre-change CLI on fixtures incl. rotation and single-page (`cmp`); 3 unit tests (structure, determinism, subset+rotation). Round-62 |
| PDF_MASK_VISIBILITY (#620, #621) | 2026-07-11 | PDF export (mask presence/visibility) | **Fixed** | bilevel fixtures blank → **pixel-perfect** (ΔE 0, SSIM 1.0); malliavin/problem_page recover their entire text layer; carte ΔE 20.3→17.7 with −29% size, colorbook −28.5% | Three defects: (1) `collect_mask_stream` was inline-Djbz-only → shared-dict (DJVI) docs had zero mask streams — now routed through `extract_mask` (also adds Smmr); (2) bilevel `/Im0` ImageMask painted `1 1 1 rg` = white-on-white → every bilevel-only page blank in poppler/Ghostscript/Quick Look — now `0 0 0 rg`; (3) FG44 pages: stencil painted in mean FG44 colour when near-uniform (spread ≤48/255, keeps watchmaker crisp + PDF_G4 win), skipped when multi-coloured (fidelity over crispness; true MRC → #563). czech (external-DJVI indirect doc) unchanged — pre-existing limitation. Round-60 |
| FGBZ_FROM_BLITS (#612) | 2026-07-11 | colour encode (FGbz palette build) | **K** | **−35.0%** `encode_color_page_quality_bgheavy` (52.6→34.2 ms); **−25.4%** `encode_color_page_quality`; `jb2_encode_dict` unchanged | `encode_jb2_dict_with_blits` hands the emitted blits (shape+position, emission order, move-only) back to the caller; `foreground_fgbz_from_blits` accumulates palette colours straight off them — no decode of the just-encoded Sjbz, no page rescan; bundle path also stops decoding the shared Djbz back. Decode-based path kept only for lossy rec-7 (`lossy_threshold>0`, near-twin pixels differ). Byte-identical CLI encode across quality/archival/lossless × single/multi-page. Round-63 |
| C5_SUB4_MASK (#607) | 2026-07-11 | render cache (downgrade tier, JB2 pages) | **K** | sub=4 re-render after downgrade **11.88→3.96 ms (−67%)**; sub=1/sub=2 rails and cache ceiling unchanged | `downgrade` keeps `mask_sub4` (~1/16 packed mask, already in `cached_bytes`); `decode_layers` bypasses the full JB2 decode when sub≥4 ∧ bold=0 ∧ no FGbz ∧ warm `mask_sub4_cached()` — compositor reads only the sub4 plane there, pixel-identical by construction. Structural `JB2_MASK_DECODES` counter proves 0 decodes on the warm path, cold full-res unchanged, bold excluded. Round-65 |
| FUZZ_ENCODER_GAPS (#567) | 2026-07-11 | fuzz infra (bzz encode, G4 encode, lossy JB2 opts) | **K** (infra) + **Fixed** | found+fixed: `bzz_encode` emitted undecodable streams for ≥4 MiB inputs (block split at MAX_BLOCK_SIZE, wire block = input+1 marker > decoder cap); local libFuzzer blocked (macOS dyld/ASan hang) → committed randomized soak tests + weekly CI fuzz cover the targets | New `fuzz_bzz_encode` (round-trip) and `fuzz_g4` (structured bitmap → encode_g4 → own T.6 decode, pixel-exact) targets + lossy-options fuzzing in `fuzz_encode` (decodable, dimension-exact, despeckle never adds ink). Wired into fuzz.yml and oss-fuzz build.sh (the latter also gained the missing `fuzz_encode`). Block-boundary unit test `bzz_roundtrip_block_boundary` (4 MiB ± 1). External libtiff differential (issue item 3) still open on #567. Round-64 |
| PDF_WRITER_STREAM (#606) | 2026-07-11 | PDF export (serialization memory) | **K** | 504-page fixture: peak RSS seq **2.72→2.19 GB**, par **2.85→2.34 GB**; wall-clock within noise; output byte-identical (20/20 fixtures + poppler-validated 504-page doc) | `PdfWriter` generic over `Write`: objects stream out as added, only (id, offset) retained; new `djvu_to_pdf_to_writer`, bytes APIs are Vec wrappers; CLI streams to `BufWriter<File>`; parallel path renders in 8×threads chunks (2× chunks cost +27% wall — tail imbalance). 1.5× total-RSS target not met: remaining memory is per-page render caches (~4.3 MB/page), unevictable behind `&DjVuDocument` → filed #629. Round-66 |
| WASM_ZERO_COPY (#611) | 2026-07-11 | wasm binding (pixel transfer) | **K** (additive) | 128 MB frames: 220.7→203.6 ms/frame (−7.7% total; the whole JS alloc + wasm→JS copy removed, **>99%** of the transfer step); per-frame JS alloc eliminated; stale views detach (len 0) after memory growth — verified in a real Chrome tab | New `WasmPixmap` (Rust-owned buffer): `view()` zero-copy `Uint8ClampedArray` (documented lifetime contract, single `unsafe` exception), `to_bytes()` owned copy; `render_into_pixmap`/`render_progressive_into_pixmap` reuse the allocation via `render_into`. Copying APIs unchanged (Node-safe default). Committed bench: `examples/wasm/bench_zero_copy.html`. Round-67 |
| WASM_LAZY_OPEN (#609) | 2026-07-11 | wasm binding (document open) | **K** | 504-page bundle: `from_bytes` median 1.00→0.60 ms (**−40%**, steady tails 0.9–1.3→0.4–0.7 ms); open→first-render no regression (noise); real-Chrome bench | `WasmDocument::from_bytes` now takes `Vec<u8>` (same JS signature) and moves it into the shared `Backing` (`parse_backed`) — bundled pages materialize lazily, same path as native LAZY_PAGE_CONSTRUCT; eager per-page copies at open eliminated. Committed bench `examples/wasm/bench_open.html`. Round-68 |
| FGBZ_PDF_STENCIL (#559) | 2026-07-11 | PDF export (coloured foreground) | **Fixed** | rasterized-PDF vs our render: irish ΔE mean **8.10→0.50**, luma SSIM 0.851→0.982; navm_fgbz better on all 6 pages; size +5.7/+7.2% on true like-for-like docs (<+10% budget); 17/17 no-palette fixtures byte-identical | One ImageMask stencil per FGbz palette colour (bbox-cropped, always-adaptive Deflate/G4, own `r g b rg` fill) instead of a single black `/Mask0`. Also surfaced+fixed for coloured pages: `collect_mask_stream` was inline-Djbz-only, so shared-dict (DJVI) docs had **zero** mask streams in PDF (navm_fgbz baseline had no text overlay at all — its +20.1% is restored content, not overhead). Legacy black-stencil path byte-identical; its shared-dict variant still drops the mask — follow-up filed. Unblocks #563. `examples/pdf_fg_color_probe.rs`. Round-59 |
| PAR_SAUVOLA (#575) | 2026-07-11 | segment (Sauvola binarization, `parallel`) | **K** | **−42.0%** on new `segment_page_color_sauvola` bench (6.28→3.64 ms) | Row-parallel `fill_sauvola_mask` via `par_chunks_mut(row_stride)`; per-row body writes packed bits directly; SAT stays serial (memory-bound). Byte-identical (dedicated non-aligned-width test + segment suite in both modes). Opt-in profiles only — default Otsu untouched; helps single-image/CLI latency, bundles already amortize per-page. Round-61 |
| PAR_PAGE_LAYERS | 2026-07-10 | encoder (single-page colour, parallel) | **K** (3rd attempt) | **−4.3…−5.2%** on new `encode_color_page_quality_bgheavy`; JB2-dominated bench unchanged | `rayon::join(Sjbz, BG44)` in `PageEncoder::encode` (`parallel` feature, byte-identical). Arc: reverted 2026-07-02 (noise — text-only fixture, jb2≫iw44) and 2026-07-04 (thermal-contaminated A/B), kept 2026-07-10 on the #496 fixture = colorbook p58 **composited** render. Key stage split there: seg 21 ms / jb2 7.8 ms / **iw44 1.8 ms** of ~40 ms — corpus has no true photo page; measure future colour-encode micro-parallelism on this bench first (this is what closed #574). Round-56 |
| CARTE_CHROMA_HEADER (#561) | 2026-07-10 | IW44 v1.2 decoder (correctness) | **Fixed** | `carte.djvu` vs `ddjvu`: mean \|Δ\| **73.76→0.53/255**; all five non-carte interop controls unchanged (mean ≤0.14) | The clear high bit of IW44 v1.2's `delay_byte` is **not** a half-resolution-chroma signal. Allocating 700×426 Cb/Cr for carte's 1400×852 background desynchronized adaptive ZP streams into plausible noise; DjVuLibre uses full-resolution planes. Decoder now always allocates full-resolution chroma for v1.2. Replaces carte's self-consistency noisy golden with a full-chroma FNV-1a pixel digest. This supersedes CHROMA_BILINEAR/#422's premise; its half-plane conversion path has no valid decoder input. Round-57 |
| BENCH_GATE (#88) | 2026-07-10 | Criterion CI regression gate | **Fixed** | missing current results now exit **2**; self-compare exits **0** | Corrected baseline root (`baseline/`, not `baseline/target/criterion`); benchmark failures are no longer ignored; compare records its exit before comment publishing and then fails on every non-zero status. `actionlint` and synthetic missing/current plus real self-compare probes pass. Round-58 |
| ZP_U64 | 2026-07-06 | ZP decoder (djvu-zp/jb2/iw44/bzz, `bit_buf` width) | **R** | no reproducible ≥3% win on either arch: M1 5-pair ratios span and cross zero (sign flips by pair 5); x86 CI same-code replay proved an 11.7% control-only noise spread and a 33-point swing on one bench from identical code | Reopened round-47's `X86_CI_BENCH` lead — widened `bit_buf` u32→u64 for bulk 4-byte refill in all 4 inline `refill!` sites, byte-exact throughout (556-page corpus digest identical at every stage, `pos`/EOF-padding semantics preserved, `ZP_EOF_SLACK_BYTES=16` margin intact). Two-stage rollout (iw44-only, then unified) both fully byte-exact and both fully reverted — decision hinged entirely on the honest multi-sample benchmark read, not correctness. Round-55 |
| IW44_ENTROPY_PROBE | 2026-07-06 | IW44 encoder (size, diagnostic) | **D** | localizes Round-52's gap to band 0/DC on small pages (`corr(bytes,ratio)=−0.591` across 62 colorbook pages, worst page 1.251×); best lever (chunking) only −0.04 to −1.7%, below the 3% bar | Instrumented `PlaneEncoder` with a thread-local, decoder-unchanged `iw44-probe` feature (`djvu_iw44::encode::probe`: per-band bytes + block-band/bucket-NEW/activate/refine call-true counts), guarded by a `probe_does_not_change_output` regression test. `examples/iw44_band_probe.rs` found watchmaker (smooth) is 98.6–99.4% band-0 at ~parity, while colorbook's worst-ratio pages (61, 2, 59, 1, 60) are >79% band-0 vs its best-ratio pages (35, 36, 25, 21, 54) at ≥55% mid+fine share — reframing round-22's "textured" label as "small/DC-dominated," not "high-frequency." Candidate (a) band-dependent activation threshold: already fully closed by IW44_ACT_THRESH (round pre-5), zero slack confirmed. Candidate (b) coefficient pre-quantization: inherently PSNR-coupled (forward DWT is a tested exact i32 mirror), out of scope for a zero-PSNR probe, not attempted. Candidate (c) chunk/slice boundary layout (`slices_per_chunk`): tested {10,25,50,74,99} via `examples/iw44_chunking_lever.rs` (ddjvu-validated, 3 full pages, 0 maxΔpx) and `examples/iw44_chunking_sweep.rs` (96 real BG44 pages across 3 corpora, bit-identical decode asserted) — real but small savings (0.04–1.7%, best on small smooth pages), non-monotonic, interop-safe, left as an existing option (`Iw44EncodeOptions::slices_per_chunk`), not a new default. Root cause of the residual band-0 gap (likely encoder-vs-DjVuLibre coefficient-value differences in the forward wavelet transform) not validated — no DjVuLibre C++ source available locally to diff. Round-51 |
| TXTZ_OCR | 2026-07-06 | encode (`PageEncoder`, opt-in text layer) | **K** (opt-in) | +2,682 B (2.62 KB) TXTz cost on watchmaker.djvu page 0 (2550×3301); 242 word zones round-trip with sane coords | New `PageEncoder::with_text_layer`/`with_ocr_text_layer` (`src/djvu_encode.rs`) — runs an `OcrBackend` on the page image during encode and appends a BZZ-compressed `TXTz` chunk; default omits it entirely (byte-identical, tested). Reused, not rebuilt: `OcrBackend::recognize` already returned the full page→line→word zone tree (PR #125), `text_encode::encode_text_layer`/`bzz_encode` already serialized it (used by PR #315's post-hoc `djvu ocr --output` mutation path) — the actual gap was `PageEncoder` having no text-layer awareness at all. New `bitmap_to_pixmap` expands bilevel sources for OCR (mirrors `examples/ocr_qa.rs`'s `mask_to_pixmap`). Real Tesseract 5.5.2 run on `watchmaker.djvu`: OCR text reads correctly, decodes back through `DjVuDocument::text_layer()`, and exports to a `/Font`-bearing PDF via the existing `pdf_with_text_layer` path with no changes. New `examples/txtz_ocr_demo.rs` + 6 unit tests. Round-51 |
| PDF_G4 | 2026-07-06 | PDF export (bilevel mask encoding, opt-in) | **K** (opt-in) | mask-only: 1.6–2.31× smaller on 3/4 corpus docs (wash on halftone-heavy content); whole-file up to −43.1% combined with `adaptive_raster` | Round-4-deferred lever: JB2 masks re-encoded as generic Deflate raster in PDF export, never exploiting row-to-row redundancy. New `smmr::encode_g4` — full T.6 2D encoder (pass/vertical/horizontal modes, changing-element arrays + `partition_point` binary search) producing PDF-`CCITTFaxDecode`-ready payloads; wired into `pdf::collect_mask_stream` behind `PdfOptions::ccitt_g4` (default off, byte-identical), "encode both keep smaller" pattern like round-28's `adaptive_raster`. First attempt reusing the existing H-mode-only `encode_smmr` was 2–4× *larger* than Deflate — the whole win needs real vertical/pass mode, hence new code. **Found and fixed a real, pre-existing T.6 spec bug** while validating externally (poppler `pdftoppm`, libtiff via Pillow): `find_b1` (decoder, used for production `Smmr`-chunk decoding too) and the new `find_transition` (encoder) searched the reference line's next changing element *inclusive* of the current coding position `a0` instead of strictly after it — spec-equivalent only for a row's first element, wrong on every later step, which is exactly when vertical/pass mode matter. Both sides shared the bug symmetrically, so round-tripping through this crate's own decoder never caught it; a previous code-review comment had flagged and reverted an `a0+1` fix because it broke a hand-crafted decoder test whose *expected* bitstream had itself been hand-reasoned under the same wrong assumption. Fixed via a uniform `i64` sentinel-`-1` convention in both `find_b1`/`decode_row_pixels` and `find_transition`/`encode_row_2d`; rewrote the one broken hand-crafted test with a non-colliding case, bitstream generated via `encode_g4` instead of hand bit-twiddling. Re-validated: 7/7 synthetic bitmaps byte-identical via libtiff (previously 6/7); zero CCITTFax syntax errors and pixel-identical (`compare -metric AE` → 0) rasterization across all pages of all 4 target corpus PDFs via `pdftoppm` (previously dozens of syntax errors per file). 51 `smmr` + 5 new `pdf` tests; full `cargo test --lib` (723) and `make check` green. Round-53 |
| INTEROP_STREAMS | 2026-07-06 | IW44/JB2 stream handling (interop vs DjVuLibre) | **Fixed** (2a, 2b-serial) / **Benign, documented** (2b-swallow, 4) | fuzz seed 42: `our-renders-what-they-reject` 310→241 (−69, moved to `both-render-fail`); 0 new crashes | Round-50 DIFF_FUZZ follow-up on findings 2 & 4 (findings 1/3 = separate INFO_VERSION effort, out of scope). Added `Iw44Error::UnexpectedSerial` + `next_serial` continuity check in `Iw44Image::decode_chunk` (`crates/djvu-iw44/src/lib.rs`), mirroring DjVuLibre's `cserial`/`wrong_serial` check — round-26 BUG-ZPSHORT empty-payload tolerance for in-order chunks confirmed untouched. Added `iw44_reduction_is_legal` INFO-vs-BG44 dimension cross-check (`src/djvu_render.rs`, mirrors `DjVuFile::get_dpi`'s 1:1..1:12 reduction-ratio rule) wired into `PageLayers::bg44`/`bg44_partial`/bounded decode branch; verified against the exact round-45 `boy.djvu` 192×384-vs-192×256 case (real `ddjvu`: "Cannot decode page 1", exit 10; us pre-fix: silent exit 0, post-fix: `IW44 decode error`, exit 1). `PageLayers::bg44`'s unconditional swallow-on-error partial-decode kept as-is — pixel-diffed against clean baselines (max diff 5–26/255, <0.3% pixels) confirms graceful degradation, not garbage. JB2 "unknown record type" (finding 4) root-caused: DjVuLibre's `JB2Image.cpp` throws the identical `bad_type` hard error on out-of-range record types (fetched source, confirmed same `CodeNum(0,11,…)` construction) — no handling gap; pixel-diffed `ddjvu`'s own render on 2 repros against clean baselines (0.5%/7.6% pixels differing >10/255) proves the divergence is post-corruption arithmetic-coder chaos, not a spec gap — unfixable by alignment, recorded benign. 5 new tests. Round-46 |
| C5_COMPRESS | 2026-07-06 | memory (render cache, opt-in) | **K** (opt-in) | **~23% faster** sub=2/thumbnail re-render after eviction; same byte ceiling either way | New opt-in middle eviction tier: `PageLayers::downgrade` clears `bg44`/`bg44_partial`/mask layers/`bg_rgb_s1`/tile cache but **preserves** already-cached `bg_rgb_s2`/`bg_rgb_s4`; `decode_background_chunks` checks the downscaled cache before forcing a decode. `DjVuDocument::enforce_cache_budget_with(budget, protect, CacheBudgetOptions{downgrade_before_drop})` — two-pass sweep (downgrade LRU-first, fall back to full drop if still over budget); default `false` = byte-identical to `enforce_cache_budget`. Design B (DEFLATE compress-on-evict via `miniz_oxide`) **rejected**: round-trip 32–260 ms vs 10–58 ms cold decode. Ruled out: keeping `bg44` coefficients doesn't save memory vs the RGB pixmap (`PlaneDecoder` allocates full-size on chunk 0). `examples/c5_compress_bench.rs`. Round-48 |
| PY_CI | 2026-07-06 | djvu-py (test/CI infra) | **K** (infra) | 41 pytest tests, all green (~3.3s); dedicated GIL-release timing test measured 5.9× speedup (gate at lenient 1.3×) | New `djvu-py/tests/` (conftest + 5 modules: document open/from_bytes/errors, render dims/non-blank/DPI-scale, text extraction, round-41's buffer protocol/zero-copy/lifetime safety, GIL-release threading) + `djvu-py/Makefile` (`py-venv`/`py-build`/`py-test`, `uv`-managed venv + `maturin develop --release`). New non-required `djvu-py` job in `.github/workflows/ci.yml` (ubuntu-latest, one Python version, cached cargo) — wired like `OCR (tesseract)`/`Coverage`: not in the branch-protection required-checks list. Follow-up from round-41 (PY_ZEROCOPY_BUFFER/PY_GIL_DETACH, #530), which shipped with only ad hoc scratchpad scripts. Round-46 |
| TH44_GRID | 2026-07-06 | render (thumbnail grid, opt-in) | **K** (opt-in) | ~1.9–14.9× (colour), ~4.1× (bilevel); SSIM 0.36–0.63 confirms D5's 0.50–0.68 at 128px | New `Document::thumbnails`/`thumbnails_with_strategy` + `ThumbnailStrategy::{Auto,Th44Only,RenderOnly}` (`src/lib.rs`), parallel under `parallel` feature. Builds the grid API D5_TH44_PREVIEW called for: `Auto` prefers embedded `TH44`, falls back to a byte-identical real render when absent. Structural test corrupts every `Sjbz` payload in a synthesized bundle to prove `Th44Only`/`Auto` never touch JB2/BG44 decode (`RenderOnly` fails on the corrupted data, `Th44Only`/`Auto` don't). No corpus file embeds `TH44` (per D5) — benched/measured on bundles synthesized with the #476 thumbnail encoder. Round-44 |
| OCR_QA | 2026-07-06 | JB2 lossy levers (quality infra) | **K** (infra) | `lossy_text()` (2%) OCR-invisible (100/100% char/word agree) on both corpora; SSIM and OCR **disagree past that point in both directions** | New `examples/ocr_qa.rs`: OCRs (Tesseract, via the existing `OcrBackend` seam) the lossless vs. lossy JB2-mask decode at the same operating points as `jb2_lossy_b0`/`jb2_despeckle` (`lossy_threshold`∈{2,5,8,10}%, `despeckle=8`), scores Levenshtein char/word agreement (lossy-OCR vs lossless-OCR, not ground truth). Real Tesseract run (macOS, tesseract 5.5.2+leptonica 1.87.0): scan corpus `lossy 8%` already shows a real SSIM dip (0.9956) at **100% OCR agreement** (SSIM over-warns); text corpus `lossy 8/10%` shows word-agreement (99.86/99.58%) falling faster than SSIM (0.9986/0.9985) — OCR is the more sensitive instrument there. Both metrics agree at the shipped 2% preset. Graceful fallback (`n/a` columns) without `ocr-tesseract`/a working Tesseract install; 11 unit tests against a mock `OcrBackend` need neither. No shipped-code change. Round-43 |
| CARTE_INFO_TRUNCATED | 2026-07-06 | djvu-iff / INFO chunk parse | **Fixed** | `carte.djvu` (skipped as "truncated" since Round-42) now parses — root cause was a too-strict 10-byte minimum on `PageInfo::parse`, not a corrupt fixture | `carte.djvu`'s `INFO` chunk is a real-world 5-byte short form (width, height, version byte; no dpi/gamma/flags) — DjVuLibre (`djvudump`/`ddjvu`/`djvused`, all confirmed on this file) tolerates it, defaulting the missing fields; our parser hard-required 10 bytes. `src/info.rs`'s `PageInfo::parse` now requires only 4 bytes (width+height) and defaults dpi=300/gamma=2.2/rotation=None when the chunk ends early. Tests: `carte_style_five_byte_info_parses_with_defaults`, `nine_bytes_parses_dpi_and_gamma_defaults_only_flags`, `djvu_document::tests::parse_carte_with_short_info_chunk`. The then-separate render defect is resolved by `CARTE_CHROMA_HEADER` (#561, round-57). Round-41 |
| MMAP_TEST_THREADLOCAL | 2026-07-06 | test infra (`djvu_render` progressive decode count) | **Fixed (test)** | `progressive_decoder_chunk_decodes_are_on_not_on_squared` failure reported "under `--features mmap`" was actually caused by `parallel`, not `mmap` — bisected and reproduced with `parallel` alone | `decode_layers` runs the naive session's BG44 decode inside `rayon::join` (`#440`/PAR_DEC); calling `join` from a non-worker thread (the test thread) bridges execution onto a rayon *global-pool* worker thread, so the `#[cfg(test)]` `thread_local!` decode counter set/read on the test thread never sees the increments (confirmed via `ThreadId` instrumentation — 3 different worker threads across one test run). Behavior (O(N) vs O(N²) decode counts) was already correct; only the counting mechanism was thread-blind. Fix: run the measurement inside a dedicated single-worker `rayon::ThreadPoolBuilder` pool (`#[cfg(feature = "parallel")]` only) so any bridged `join` resolves on that one worker, keeping counter set/read on one thread without polluting/being polluted by other concurrent tests' decode calls. Verified green under `parallel`, `cli,mmap,parallel`, and default; full suite 693/693 under `cli,mmap,parallel`. Round-42 |
| PY_ZEROCOPY_BUFFER | 2026-07-06 | djvu-py (Python bindings, `Pixmap`) | **K** | `data()`/`to_numpy()`/`to_pil()` copy 23–51 ms per call on a 248 MB page; zero-copy equivalents ~0.001–0.005 ms | `Pixmap::__getbuffer__`/`__releasebuffer__` (flat read-only buffer, pyo3's own tested `test_buffer_protocol.rs` pattern) makes `memoryview(pixmap)`/`bytes(pixmap)`/`numpy.frombuffer(pixmap,...)` work directly with no copy; buffer holds an owned ref so the `Pixmap` outlives any view into it. Additive `to_numpy_zerocopy()`/`to_pil_zerocopy()` convenience wrappers, byte-identical output to the existing (untouched) `to_numpy()`/`to_pil()`. Round-41 |
| PY_GIL_DETACH | 2026-07-06 | djvu-py (Python bindings, render/open/text) | **K** | 2 threads **1.92–1.93x**, 4 threads **3.29–3.91x** page-render speedup | `py.detach()` (pyo3 0.29's renamed `allow_threads`) wraps `Document::open`/`from_bytes` and `Page::render`/`text` — previously ran fully under the GIL. Safe with no `unsafe`: core `Document` is compile-time asserted `Send+Sync` (`src/lib.rs:625`), `Page`/`Pixmap` have no interior mutability. `Document::page()` left alone (confirmed cheap, metadata-only borrow). Measured via 8-page `threading.Thread`-sharded render on `colorbook.djvu`. Round-41 |
| WASM_THREADS | 2026-07-06 | wasm (opt-in threads) | **D-infra** | buildable (nightly + build-std) but +1cores≈noise (native dpi), **9× SLOWER** on downscale-compositor path | New opt-in `wasm-threads` feature (`wasm` + `parallel` + `wasm-bindgen-rayon`) layers a Web Worker rayon pool over the existing PARALLEL/IW44_PAR code paths. Confirmed real, working nightly build (`-Z build-std=panic_abort,std`, `+atomics,+bulk-memory` + `--shared-memory`/`--import-memory`/TLS-export link-args — plain `+atomics` alone throws `DataCloneError` postMessage-ing the Memory) and validated live in an actual Chrome tab (COOP/COEP server) against `colorbook.djvu`. Measured, not simulated: native-dpi full IW44 decode (2260×3669) single 55.8–57.3 ms vs pool=10 57.0–54.8 ms (within noise, no reliable win — only 3-way rayon::join in IW44_PAR, not enough parallel granularity to amortize Worker dispatch); 150-dpi downscale-compositor path single 18.6 ms vs pool=10 **167 ms** (9×slower — many small `par_chunks_exact_mut` chunks, Worker/Atomics sync cost dwarfs the tiny per-chunk work). Default `wasm` build untouched (stable `cargo check --target wasm32-unknown-unknown --features wasm` still green); not part of any required CI gate — see `make wasm-threads-check` |
| C4_TILE_CACHE | 2026-07-06 | render (pan/zoom viewer) | **K** (opt-in) | pan sequences save **~20–25% compositor time** on both colour and bilevel pages at 2×/4× zoom | New `render_region_tiled` (std-only, separate entry point — `render_region` untouched, zero overhead for existing callers): assembles a region from `TILE_SIZE=256` composited RGBA tiles cached per-page (`PageLayers::tile_cache`, FIFO, `TILE_CACHE_MAX_BYTES=8MiB`), folded into C5's `cached_bytes`/`enforce_cache_budget` (no new LRU). Byte-identical by construction (`composite_into` keys off absolute pixel position) + test (`render_region_tiled_matches_render_region` et al). Eligible only for Bilinear + identity rotation + strict decode; else falls back to `render_region`. Justified by VIEWER_BENCH. Round-38 |
| VIEWER_BENCH | 2026-07-06 | render (bench infra) | D (infra) | pan step redoes ~75–100% of viewport work per step with no cache; incremental-strip proxy is 20–25% cheaper | New `benches/viewer.rs`: scripted open→full render→zoom2×→12-step 25%-overlap pan→zoom4×→pan, on a colour page + a bilevel page. `full_recomposite` vs `incremental_strip` (proxy for a tile cache) via existing `render_region`. Whole-sequence totals: colour 2× 5.37→4.25ms (−20.9%), colour 4× 21.89→16.78ms (−23.3%), bilevel 2× 5.46→4.32ms (−20.9%), bilevel 4× 20.90→16.78ms (−19.7%). Motivated C4_TILE_CACHE. Round-38 |
| IW44_RATE_TARGET | 2026-07-06 | IW44 encoder (byte-budget target) | D (already shipped) | `Iw44Target::Bpp` was already merged (#475, 2026-07-01) — dangling branch ref, not abandoned work | Task asked to "resurrect" `feat/iw44-quality-target`, presumed unmerged (no open PR). Found: PR #475 from that exact branch is MERGED (squash `a1e3d54b`, confirmed in `main`'s ancestry); the remote branch ref was just never deleted. `Iw44EncodeOptions::target: Iw44Target::{Slices, Bpp(f32)}` already wired through both `encode_iw44_color`/`encode_iw44_gray`, default `Slices` byte-identical, CLI `--bpp` flag, 6 existing unit tests. The brief's "ideally a PSNR floor" restates IW44_ENTROPY_GAP's (Round-37) own already-recorded verdict — a fixed budget can't capture the 30× smooth-vs-textured saturation spread, a real quality floor needs a content-adaptive stop, "~100B/page, low EV" — so not built (would contradict standing analysis, not a quick win). Added `tests/iw44_rate_target.rs`: determinism, budget-within-one-slice (via caller-set `slices_per_chunk=1`), a real 3-corpus sweep (watchmaker/colorbook/conquete_paix, bpp∈{0.05..1.0}, bytes+PSNR monotone, default-unchanged), and truncated-stream progressive round-trip. No shipped-code change. Round-36 |
| QUALITY_COLOR | 2026-07-06 | quality infra (colour-aware D1) | **K** (infra) | FGBZ_MEDIANCUT k=2 rerun: luma SSIM −0.0056 (looks fine) vs **Cr SSIM −63%** (0.880→0.326), ΔE mean doubles (12.4→25.6) | New `quality::compare_color` (`src/quality.rs`): YCbCr per-channel SSIM (Y/Cb/Cr + 0.8/0.1/0.1-weighted combined) + mean/max CIE76 ΔE, additive to the untouched luma-only `psnr`/`ssim`/`compare`. Closes the round-31 FGBZ_MEDIANCUT honesty caveat ("D1 harness is luma-only, chroma blind") — re-running that experiment's synthetic fixture at k=2/4 shows the new metric catching a colour wash-out luma-SSIM misses entirely; recovers to baseline at k≥6, agreeing with round-31's crop-inspection conclusion. Synthetic isoluminant unit test (same-luma, shifted-hue solid fields) proves the discrimination directly. Wired into `examples/quality_harness.rs` and `examples/fgbz_mediancut_harness.rs` as extra columns. No new deps (std-only f64 math, module already `#[cfg(feature = "std")]`-gated). Round-36 |
| COLD_OPEN_HARNESS | 2026-07-06 | cold-open measurement (infra) | **K** (infra) | cold/warm ratio **4.76x** (page 0, F_NOCACHE fresh-copy strategy) | `examples/cold_open_bench.rs` — unblocks round-8 "B6/B7 need a cold-disk harness". macOS has no `posix_fadvise(DONTNEED)`; copies corpus to a fresh temp path per iteration via an `F_NOCACHE`-flagged fd so the copy's pages aren't cache-resident. `sudo purge` documented as manual cross-check. Median+MAD stats. `--mode validate\|madvise\|prefetch`. Round-40 |
| B6_MADVISE | 2026-07-06 | mmap cold-open (readahead hint) | X (neutral; kept opt-in) | scoped page-range hint: −1.6% (noise); naive whole-prefix hint: **reproducibly −12%** | `MmapDocument::advise_page_willneed` via `memmap2::Mmap::advise_range(WillNeed)` (safe wrapper, no unsafe). Whole-`0..range.end`-prefix version regressed cold open ~12% (over-advising is actively harmful, not just wasted) — corrected to the target page's own FORM range only, which is then a wash: `MmapDocument::open()` already pays the structural DIRM/page-range walk before the hint can be issued, and per-page ranges are small vs fast local NVMe demand-fault latency. Kept as tested opt-in primitive (D/F status, like AVX2_IDWT) — may win on higher-latency storage. Round-40 |
| B7_PREFETCH_PAGE | 2026-07-06 | mmap page-turn latency (background decode) | **K** (opt-in) | **+24–90%** faster next-page render (dwell-dependent); dwell=0 correctly shows no win/regression | `DjVuDocument::prefetch_page(self: &Arc<Self>, idx)` (`parallel` feature): `rayon::spawn`'d background decode into the existing `OnceLock`-backed `PageLayers` cache (no new cache, no unsafe, `OnceLock::get_or_init` dedups vs a later sync render). Page0/dwell150ms: +23.8% and +26.5% (two runs); page100/dwell300ms: +89.7% (heavier baseline decode, more time to hide it); dwell=0 negative control: −0.7% (noise) — honest disclosure that benefit needs dwell ≳ decode time. Respects C5 cache budget (same cache, same eviction). Round-40 |
| X86_CI_BENCH | 2026-07-06 | CI infra (x86 bench gating, `bench.yml`) | **K** (infra) | first real x86 numbers: ZP-decode-only benches **−4 to −28%** faster under `-C target-cpu=x86-64-v3`; IDWT-inclusive `to_rgb` benches **+3 to +14%** regressed | Confirmed the "Benchmark (x86-64-v3 AVX2 validation)" job's PR-SKIPPED badges are by-design (detect job only runs it on PRs touching IW44/SIMD files, per #363), not rot — already `success` on every push to `main`/tags; `workflow_dispatch` was already wired (unused until this round). Widened the job's `iw44_to_rgb\|iw44_decode` bench filter to add `jb2_decode\|bzz_decode` so the ZP-decoder `refill!` hot loop (shared, separately inlined, across jb2/iw44/bzz) is measured on x86 too, alongside the already-covered IDWT path (`to_rgb`→`inverse_wavelet_transform`). Dispatched a real run ([28784274181](https://github.com/matyushkin/djvu-rs/actions/runs/28784274181)): ZP-entropy-only benches (`jb2_decode`, `jb2_decode_corpus_bilevel`, `jb2_decode_large_600dpi`, `iw44_decode_first_chunk`, `iw44_decode_corpus_color`, `bzz_decode`) all get faster under `x86-64-v3` codegen — concrete positive first data point for **ZP-U64** (round-3/4 deferred); `iw44_to_rgb_colorbook` (which runs the actual IDWT row/col pass) instead *regresses* — shows **AVX2_IDWT** (round-5 #8) needs a real hand-written SIMD path, not just a codegen flag, before it can win. Ratio methodology (same runner, back-to-back, default vs `x86-64-v3`, Δ%) already matches this repo's same-run noisy-cloud-runner pattern — documented, unchanged. No source/runtime code touched. Round-47 |
| DIFF_FUZZ | 2026-07-06 | fuzz infra (differential vs DjVuLibre) | **K** (infra) | 2100 mutants, **0 crashes/timeouts**; 5 confirmed semantic-divergence classes (none fixed — no small/obvious fix without spec-level judgment) | New `examples/diff_fuzz.rs` + shared `examples/support/mod.rs` (diff logic deduped out of `interop_pixdiff.rs`): deterministic (splitmix64) corpus-mutation driver (truncate/bitflip/resize-length ops) over 3 corpus files, classifying each mutant on a symmetric 3-level ladder (structural-reject/render-fail/rendered) computed independently for us and DjVuLibre (`djvudump`+`ddjvu`) — catches cases a binary accept/reject compare misses (djvudump structurally accepts but ddjvu's own decoder still refuses). 2100 mutants/153s: both-reject 1266, both-accept-match 429, our-renders-what-they-reject 310, both-render-fail 63, our-laxer 19, our-render-fail 4, our-stricter 4 (verified NOT a bug — we validate outer FORM's declared length upfront, djvudump doesn't), pixel-mismatch 4, dim-mismatch 1. Confirmed findings recorded as follow-ups: INFO version-ceiling not enforced, BG44/IW44 truncated-stream tolerance (+ INFO-vs-payload dimension cross-check gap), INFO minor-version byte affecting rendered pixels on both sides differently, JB2 "unknown record type" divergence (unconfirmed root cause). Repros under `fuzz/corpus-regressions/diff_fuzz/`. Round-45 |
| INTEROP_INFO | 2026-07-06 | djvu-iff / INFO chunk parse (interop) | **Fixed** | DIFF_FUZZ pixel-mismatch 4→0, dim-mismatch 1→0; `our-renders-what-they-reject` −35 (moved to `our-stricter`, matching DjVuLibre's own renderer) | Fixes Round-49 findings 1 & 3. (1) Version ceiling: DjVuLibre's `DJVUVERSION_TOO_NEW=50` (`libdjvu/DjVuInfo.h`, enforced in `DjVuFile::decode_chunk`) wasn't enforced by us — added identical `>=50` rejection (new `IffError::UnsupportedVersion`) in `PageInfo::parse` (`src/info.rs`), version computed the same way DjVuLibre does (minor byte, optionally combined with major byte unless major==0xff sentinel). Round-42's 5-byte short-INFO tolerance unaffected (absent version → 0). (2) Finding 3 reframed: the minor-version byte itself affects nothing pixel-wise on either side; the real driver in 4/5 boy.djvu repros was an **unclamped gamma byte** — DjVuLibre always clamps `0.1*gamma_byte` to `[0.3,5.0]` (even when present-but-zero), we didn't clamp at all. Fixed to match. The 5th repro (dim-mismatch) was a **diff_fuzz harness bug**, not a product bug: `our_attempt()` returned pre-rotation `page.width()/height()` instead of the rendered pixmap's own (post-rotation) dims; fixed in `examples/diff_fuzz.rs`. Incidental: patched a stray non-zero major-version byte in the synthetic `bgjp_test.djvu` fixture (now correctly rejected by the new ceiling, was clearly unintentional). Tests: 7 new in `src/info.rs`. `make check` 1092 passed. Round-47 |
| IW44_SLICE_RD | 2026-07-04 | IW44 encoder (slice budget) | D | default well-tuned; no change | RD curve via D1: past 100 slices PSNR pinned (22.42/39.03 dB), SSIM +≤0.003 while size grows 2–25× (colorbook 100→150 = +1.8 MB for +0.0003 SSIM). total_slices=100 sits at the knee. Refutes IW44_DIAG "starved in 100 slices" — avg_abs=8.72 is the transform's quantization floor, not slice starvation. BG44 lever was BG_DIFFUSE (r17, smaller input), not the slice schedule. Round-35 |
| INTEROP_ENCODE | 2026-07-04 | encoder interop (infra) | **K** (infra) | **21/21 our encodes decode in ddjvu**; real-scan fidelity mean<0.3 | `examples/interop_encode.rs` — encode → ddjvu-decode gate (unblocks masked-wavelet: "ddjvu must accept our stream"). Encode-side complement to interop_pixdiff/diff_djvulibre (decode-side). Gate: non-zero exit if ddjvu rejects. Real colour scans mean|Δ|<0.3 vs ddjvu; degenerate re-encodes (bilevel/photo-as-colour) show mask-AA edge diffs (follow-up). Round-34 |
| LANCZOS_RGBA_ACC | 2026-07-04 | Lanczos-3 resampler | **K** | **−22 to −27% Lanczos cost** (auto-vec, bit-identical) | Both separable passes accumulated 3 scalar r/g/b from a stride-4 RGBA read, blocking LLVM auto-vec. Restructure: horizontal → `[f32;4]` per pixel over 4 contiguous bytes/tap; vertical → single interleaved `acc[dw*4]` contiguous SAXPY. Alpha lane accumulates 255, ignored. Bit-identical (digest 1154d165a6a72d7d). No unsafe / target_feature → holds on all auto-vec targets (unlike manual-NEON dead-ends #3). On top of PAR_LANCZOS + LANCZOS_HOIST. Round-33 |
| SAME_SIZE_REC6 | 2026-07-04 | JB2 encoder (lossless size) | **K** (experimental) | watchmaker Sjbz **−11.67%** @frac 2% (−11.2/−10.65 @5/8%); pathogenic ≈0…+0.5%; all round-trips pixel-exact | A0 population gate (39.6% of text fresh CCs have a ≤5% same-bbox twin, median 0.6%; scans thin: 0.9%, median 12.9%) → A1/A2 emitter `Jb2EncodeOptions::same_size_rec6` — same-bbox lossless rec-6 refinement. First lever to beat DjVuLibre glyph matching losslessly (cross-size rec-6 #322 *lost* +4.37% on the same corpus). A3 maintainer decision: stays behind `experimental`, default byte-identical. Auto-policy: JB2_AUTO_REC6. Rounds 17–18 |
| LOSSY_B0 | 2026-07-04 | JB2 encoder (lossy sweep) | D | text: **−21.96% Sjbz @thr 2%** (SSIM 0.99928), saturating ≈−24% @10%; scan: ≈0 below 8%, −11.23% @10% (SSIM 0.98871) | D1-harness sweep of the already-shipped same-size lossy rec-7 `lossy_threshold` (#224). Key finding: the big text lever already ships, just off by default; **diminishing returns above 2%**. Round-19 |
| LOSSY_B1_PROBE | 2026-07-04 | JB2 encoder (cross-size lossy population) | D | cross-size (±2 px bbox, ≤5% resampled Hamming) adds 16.2% of text fresh CCs, 2.6% on scans | Also pinned the rec-7 layout constraint: decoder sizes coords by `dict[index]` dims → a cross-size copy must code by the *twin's* dims or every later glyph shifts. Round-19 |
| LOSSY_B1 | 2026-07-04 | JB2 encoder (cross-size lossy rec-7) | **R** | dominated: same-size 2% + cross 5% = −24.81% @SSIM 0.99639 vs plain same-size 5% = −23.39% @0.99889; cross-size standalone −8.7%, worse both ways | Emitter implemented correctly (twin-dims layout, SSIM ≥0.989 everywhere) then reverted: every operating point matched/beaten by a higher same-size `lossy_threshold` with none of the machinery. **Recorded so cross-size / near-size lossy matching is not re-attempted** (this is what closed #564). Round-19 |
| LOSSY_B_SHIP | 2026-07-04 | JB2 encoder (preset) | **K** | — | `Jb2EncodeOptions::lossy_text()` (0.02) + `with_lossy_threshold` + documented operating-point table. **Closes the JB2 size-gap plan**: same-size lossless rec-6 (experimental) + same-size lossy preset; cross-size dead in both lossless (#322) and lossy (LOSSY_B1) form. Round-19 |
| GRAY_DIRECT_E2E | 2026-07-04 | render (gray output) | X | — | Wiring `to_gray8_subsample` into `render_gray8` rejected: would fork all three tuned RGBA row writers (P2/G1/F2/area-avg — the most-optimised code in the repo) and silently change the Rec.601 contract (DjVu Y ≠ Rec.601-of-RGB). Codec-level GRAY_DIRECT stays the interface via `page.decoded_bg44()?.to_gray8()`. Round-14 |
| D3_BICUBIC | 2026-07-06 | render FG44 upsampling (quality) | X (no perceptible gain) | SSIM ≥0.9982 bilinear-vs-bicubic (FG-only isolated), 0.9999 whole-page; ~17% slower when enabled | Catmull-Rom bicubic swap for `sample_bilinear` in the compositor's B-series (upscale) FG44 path; measured 12.0× FG44 subsample ratio is already below the perceptual floor under bilinear. Byte-identical default, macro-hoisted flag (zero cost off), but rejected on quality grounds — not shipped, reverted from `src/djvu_render.rs`. Crops visually indistinguishable. Round-32 |
| FGBZ_MEDIANCUT | 2026-07-06 | FGbz encoder (foreground palette) | **K** (opt-in) | synthetic fixture: FGbz −96.4% (2255B→82B), total −24.1% at k=6, SSIM −0.0031 (wash); at k≥true-colour-count SSIM matches baseline | Closes Round-31 backlog item. `foreground_fgbz` (`src/djvu_encode.rs`) previously emitted one exact palette entry per distinct per-blit average colour, bloating on scanner-noise/AA near-duplicates. New `FgbzPaletteOptions::MedianCut{max_colors}` via `PageEncoder::with_fgbz_options` runs classic median-cut quantisation + nearest-colour remap; default stays `Exact`, byte-identical (enforced by test `fgbz_mediancut_is_opt_in_default_stays_exact`). `irish.djvu` real fixture (already near-minimal 40-entry palette) shows small win only. D1 harness is luma-only (chroma blind) — compensated with before/after crops in `_pr_assets/`, visually indistinguishable at k=6/32. Not defaulted: picking `k` is content-dependent, too-small `k` (e.g. 4) does cost measurable SSIM. |
| JB2_DESPECKLE | 2026-07-06 | JB2 encoder (speck-removal pre-pass) | **K** (stable opt-in) | pathogenic (600 dpi scan) −0.94%/−1.59%/**−2.43%** Sjbz at despeckle=2/4/8 (SSIM 0.9995/0.9990/0.9985); watchmaker (text) byte-identical no-op at all levels | Round-30. First lossy lever to move the noisy-scan corpus at all — same-size `lossy_threshold` alone ≈0% there (round 19 B0), cross-size lossy reverted (round 19 B1, dominated). New `Jb2EncodeOptions::despeckle: Option<u32>` drops CCs with `pixel_count <= max_px` (true ink area, not bbox) before clustering/dedup; stable (not `experimental`), default `None` = byte-identical. `with_despeckle()` builder + `lossy_scan()` preset (`despeckle=8, lossy_threshold=0.02`). Punctuation/diacritic-dot survival validated by targeted test (16px dot/period survive, 1px dust removed) at every tested level. |
| JB2_DICT_ORDER | 2026-07-06 | JB2 encoder (shared dict) | D (negative) | Round-29; frequency/bucket-order permutation of shared dict: −0.05%…+0.05% (both directions), 30–60× below the ~1.5% bar | Confirmed rec-6 topological constraint (refiner must reference an already-emitted entry) is satisfied automatically for any in-block permutation, since the whole shared block is always emitted before page-local symbols. Measured real bytes (Djbz+Sjbz) on 75-symbol (`conquete_paix`, 22p) and 5,164-symbol (`pathogenic_bacteria_1896`, 517p) shared dicts; 0/539×3 round-trip failures. `NumContext` is an adaptive binary coder — it learns whatever empirical index distribution results from any fixed permutation, so static frequency/similarity reordering has no fixed-Huffman-style win to capture. No shipped-code change; recorded so dict reordering isn't re-proposed without a non-adaptive index coder |
| B5_STREAMING_DECODER | 2026-07-04 | progressive decode (API) | **K** | O(N) streaming progressive decode (render-as-you-receive) | Closes the deferred B5 stateful API. New `pub ProgressiveDecoder`: `new(page,opts)` decodes FG once, `push_bg44_chunk(chunk)` accumulates one BG44 chunk in a shared `Iw44Image` and returns the refined frame — for network viewers that render per arriving chunk (vs render_progressive O(N²) or render_progressive_all needing all chunks up front). Strict Bilinear only (Lanczos/permissive → UnsupportedOption). render_progressive_all's fast path refactored to dogfood it. Byte-identical (streaming-vs-batch test + direct vs render_progressive_step on chicken/colorbook; O(N) proven by chunk-decode counter: chicken 6→3, colorbook 10→4). Round-27 |
| B5_STATEFUL | 2026-07-06 | progressive decode (validation) | D | — | Independent reimplementation check of B5_STREAMING_DECODER: structural O(N) proof + direct byte-identity tests. No shipped-code change |
| BUG_ZPSHORT | 2026-07-06 | IW44 decode (BG44 refinement) | **K** (fix) | fixes `Iw44(ZpTooShort)` on watchmaker.djvu page 0; correctness (was silently using 2/4 chunks) | `Iw44Image::decode_chunk` hard-errored on a legitimate zero-length BG44 refinement payload; `ZpDecoder::new` requires ≥2 bytes. Pad short payloads to 2 bytes with `0xFF` (same convention `ZpDecoder::read_byte` already uses past a stream's end), scoped to djvu-iw44 only. Surfaced while validating PR #510's `ProgressiveDecoder`. Round-26 |
| JB2_AUTO_REC6 | 2026-07-06 | JB2 encoder (same-size rec-6 auto-policy) | **K** (experimental) | watchmaker −11.67% Sjbz (auto fires); pathogenic/conquete_paix byte-identical (auto stays off) | A3 follow-up of SAME_SIZE_REC6: `Jb2EncodeOptions::same_size_rec6_auto()` runs a bounded (≤1000-fresh-CC) density probe (`probe_same_size_rec6_density`) and enables `same_size_rec6=Some(0.02)` when ≥5% of sampled fresh CCs have a same-size ≤5% Hamming twin, else leaves it off. Threshold calibrated against **real emitted-byte deltas** on 4 corpora (not just population): watchmaker 39.6% density → −11.67%; cable_1973 12.4% → −0.43%; conquete_paix 1.7% → +0.49% (loss); pathogenic 0.9% → +0.00%. 0.05 sits ~2.9×/2.5× margin either side of the measured loss/win crossover. All round-trips pixel-exact. Still behind `experimental`, not default — same A3 shipping decision as SAME_SIZE_REC6. |
| PDF_ADAPTIVE_RASTER | 2026-07-06 | PDF export (colour raster) | **K** | **1.62× smaller** whole-file PDF on watchmaker.djvu (regression case), 1.05× on mixed content, unchanged when JPEG already wins | Round-28; implements PDF_DCT_PROBE's follow-up. Opt-in `PdfOptions::adaptive_raster` (default off, byte-identical). Per page, encodes both DCTDecode(JPEG-80) and FlateDecode, keeps smaller; loser dropped immediately (no O(1)-per-page memory regression, #449). Composes with the rayon parallel path (#298) — verified byte-identical seq vs parallel output. Lossless when Deflate wins, unchanged JPEG-80 when JPEG wins |
| PDF_DCT_PROBE | 2026-07-06 | PDF export (colour raster) | D | Round-23; shipped JPEG-80 default: 3.6–11.2× smaller on 2/3 pages, **3.1× LARGER** on a near-flat colour scan | Corrected a stale premise: DCTDecode is already the shipped default (#59), not Deflate. Content-dependent — found a real regression case (watchmaker p0: Deflate 245 KB vs JPEG-80 767 KB, SSIM already ≥0.999 either way). q85 measured strictly worse than q80 (larger, same SSIM). Recommend opt-in per-page adaptive Deflate/JPEG choice as follow-up — **implemented as PDF_ADAPTIVE_RASTER, round-26** |
| BZZ_ENC_DIAG | 2026-07-06 | BZZ encoder (TXTz/DIRM/NAVM/ANTz) | D | Round-23; 5–19% of a text-bearing page's encode time; real corpus blocks are 1.6–6.4 KB avg (~10.6 KB max) | Suffix sort is already `O(n log n)` prefix-doubling + counting sort (not naive `O(n² log n)`), 4 MiB block cap (= DjVuLibre MAXBLOCK). Real-text scaling 100 KB→2.25 MB measured ~n^1.3 (cache-unfriendly at scale) — SA-IS/divsufsort would help *if* blocks ever reach 100 KB–4 MB, but DjVu's actual per-page/per-chunk blocks stay in the low-KB range where absolute cost is already sub-2ms. Added `bench_bzz_encode` (none existed). Low-priority queue item, gated on a real large-single-block use case or an internal BWT-vs-MTF-vs-ZP profile split (not done here) |
| IW44_ENTROPY_GAP | 2026-07-04 | IW44 encoder (size) | D | c44 gap splits by content: smooth≈parity, textured ~1.3× | RD parity (matched SSIM, our decoder for both). Smooth BG (watchmaker): ours saturates at 30 slices/57B — beats c44 (84B@0.9976); raw slice-100 gap is over-coding past saturation (r18), needs a content-adaptive decibel target (~0.008 vs 0.265 bpp → 30× spread, fixed cap impossible), ~100B/page low-EV. Textured BG (colorbook): real ~1.3× at matched SSIM (345 vs 257B@0.9592) — interop-fragile IW44_ACT_THRESH normative territory. Round-22 |
| IW44_MASKED_TRANSFORM | 2026-07-04 | IW44 encoder (size) | X (unnecessary) | normative masked transform gains 0 after BG_DIFFUSE | **Closes IW44_MASKED_WAVELET.** Measured c44 `-mask` on raw BG ≈ c44 no-mask on our diffused BG (1471≈1445, 117=117, 2063=2063, 643≈644) → BG_DIFFUSE fully subsumes the masked lever; the normative transform would shrink BG44 by 0. Residual 1.18–1.62× gap vs c44 is entropy coding (IW44_ACT_THRESH), not masking. Big risky normative change shown not worth doing. Round-21 |
| BG_DIFFUSE | 2026-07-04 | IW44 encoder (BG size + quality) | **K** | **−52% to −97% BG44** on text scans, SSIM/PSNR up | Interop-safe down-payment on IW44_MASKED_WAVELET. Fully-masked (invisible) BG cells were filled with ink colour by default → wasted wavelet bits + dark halos. New `SegmentOptions::bg_diffuse`: harmonic (Laplace/Gauss-Seidel) fill from confident cells; enabled in Quality/Archival profiles. Strict win — smaller AND higher SSIM (colorbook 18092→16044 B, conquete 28348→6134 B, SSIM up on all). Encoder-only, decoder never sees it. `--bg-inpaint` CLI now = legacy ring. Round-20 |
| CLUSTER_BUCKET_HASH_DEDUP | 2026-07-04 | JB2 encoder (clustering) | **K** | **−81% (≈5.2×) cluster_shared_symbols (517p)** | `bucket_page_ccs` linear `packed_hamming` scan (max_diff==0, byte-exact) → `symbol_hash`-keyed `BTreeMap` + equality verify. Byte-identical (digest 1d797be493bab1fe, 5164 syms unchanged). Analog of CLUSTER_DEDUP #446 for the clustering pass. Est. was ~2% (fraction of full encode) — isolated on dense 517p corpus the bucketing scan itself dominated. New `bench_cluster_shared_symbols`. Round-15, swarm P4 |
| SHARED_DICT_CLONE_PER_PAGE | 2026-07-04 | JB2 encoder (bundle) | **K** | byte-identical; ~2.67M fewer clones (517p bundle); wall-clock ~12% but within thermal noise | `encode_jb2_dict_with_options` held `dict_entries: Vec<Bitmap>` and deep-copied the shared dict on every page (5164×517≈2.67M clones). Now `Vec<&Bitmap>` borrowing `shared_symbols`+`ccs` (both outlive the encode); helpers take `&[&Bitmap]`, no public sig change. Digest identical (conquete f2e5…, pathogenic f6ec…). Peak RSS flat (clones transient). Kept on strictly-≤-cost grounds like CLUSTER_DEDUP/PDF_STREAM. Round-16, swarm P2 |
| D4_GAMMA_DOWNSCALE | 2026-07-03 | render downscale (quality) | X (low headroom) | gamma vs linear-light SSIM ≥0.979 | Diagnostic via D1: gamma-space vs correct linear-light downscale differ negligibly on real pages (photo ≥0.997, text 0.979 worst). Hard edges live in the JB2 mask, not the smooth BG. Rejected; also deprioritises A3. Lanczos (D2) is the real text-downscale lever. Round-9 |
| D5_TH44_PREVIEW | 2026-07-03 | render (preview fast path) | X (opt-in only) | 20–30× faster but SSIM 0.50–0.68 | TH44 thumbnail preview: measured via D1. Big speed win but low fidelity + **no corpus file embeds TH44**. Viable only as explicit opt-in for thumbnail grids, not a default. Round-9 |
| C3_ZOOM_SCOPE | 2026-07-04 | render zoom/region | D | already efficient (~5.5 ns/px, flat vs zoom) | Diagnostic: region/zoom render is scoped, linear in viewport pixels, no per-call overhead, same per-pixel rate as full-page compositor. No speed win; residual is cold ROI_IDWT (rejected). Remaining zoom work is quality (mask AA). Round-14 |
| C5_LRU_BUDGET | 2026-07-04 | memory (auto LRU) | **K** | cache held under ceiling; RSS 714→255 MB | `enforce_cache_budget(max, protect)` + per-page LRU tick + `render_cache_bytes`. Automatic form of retain_render_caches: evicts least-recently-used pages to a byte budget. Byte-identical (test). C5 follow-up, round-13 |
| D_AA_ZOOM | 2026-07-06 | render mask AA (quality) | **K** (opt-in) | PSNR/MSE favour AA (+0.3–0.7dB, −6…−15% MSE), SSIM favours nearest (known blur-vs-SSIM tradeoff); visibly smoother glyph edges at 4× zoom | Resolves QUALITY_AA's deferred #13: opt-in `RenderOptions::mask_aa` (default false, byte-identical, proven no-op at scale≤1 incl. bg-subsampled-at-1:1 corner case) bilinearly interpolates JB2 mask coverage at genuine upscale only. ~2.2–2.8× slower than nearest when enabled. New `examples/mask_aa_quality.rs` (D1 SSIM harness) + `examples/mask_aa_crops.rs` (`_pr_assets/` PNG evidence). Round-25 |
| C5_RENDER_CACHE_EVICTION | 2026-07-04 | memory (long-lived viewer) | **K** | **−86% peak RSS** (714→103 MB, 62 pp) | Found: per-page render caches never evict → RSS grows ~11 MB/page unbounded (OOM risk on big books). Fix: additive `evict_render_cache`/`evict_render_caches`/`retain_render_caches`. Byte-identical (test). Backlog C5, round-12 |
| B5_INCREMENTAL_PROGRESSIVE | 2026-07-04 | progressive decode | **K** (modest) | 1.04–1.50× (grows w/ chunk count) | `render_progressive_all` decodes FG once + accumulates BG chunks in one Iw44Image instead of O(N²) per-frame re-decode. Byte-identical (test). Modest because IDWT reconstruct+composite are inherently per-frame; only ZP redundancy removed. Streaming-step case still needs stateful decoder API. Backlog B5, round-11 |
| PAR_LANCZOS | 2026-07-04 | Lanczos-3 resampler | **K** | **−69% (3.2×)** large-page Lanczos (parallel) | Row-parallel both separable passes behind `parallel` feature (par_chunks_mut + for_each_init scratch). Bit-identical. Isolated on colorbook native→½: 100.5→31.2ms. Amplifies D2 (Lanczos now ~3× not ~10× a bilinear downscale). Backlog A4, round-10 |
| QUALITY_HARNESS_D1 | 2026-07-03 | quality infra (unblocks D branch) | **K** (infra) | perceptual gate + D2 decided | New `src/quality.rs` (PSNR/SSIM/`compare`, 6 tests) + `examples/quality_harness.rs` (vs ddjvu). Immediate finding: Lanczos3 downscale SSIM 0.993/0.989 vs Bilinear 0.959/0.861 → **Lanczos decisively better** (#423/D2 answered). Gates A3/D3/D4/D5. Round-9 |
| GRAY_DIRECT | 2026-07-03 | IW44 decode (gray axis) | **K** | **−71% seq / −46% par** gray decode of colour page | New `Iw44Image::to_gray8[_subsample]`: reconstruct Y plane only, skip both chroma IDWTs + YCbCr math. Grayscale imgs byte-identical to `to_rgb().to_gray8()`; colour = DjVu luma (mean<4/255 vs Rec.601). Additive, no risk. Round-7, backlog B2 |
| TRIAGE_ROUND8 | 2026-07-03 | breadth triage (axes A–D) | D | verdicts for ~25 proposals | B3 (early wavelet stop) + B4-hot (word blit) **already implemented**; **C1 = RGB-writing compositor** deferred/ruled-out wide-surface (every fast path emits RGBA, SIMD tables assume 4-byte px; exports already strip per row via `rgba_row_to_rgb` — but note EPUB/CBZ PNGs don't use it, → issue #599); **C2 = fuse rotation into compositor** ruled out (strided scatter writes = the exact pattern ROTATE_TILE removed; the saved alloc is cheap — this closed #560); B5 (progressive O(N²) decode) **top opportunity, measured 4.8× on colorbook, deferred → needs stateful decoder**; rest NEEDS-INFRA / QUALITY-GATED (D1 harness unblocks D branch). See PERF round 8 |
| GATHER_ZIGZAG_INV | 2026-07-03 | IW44 encode | **K** | ~1% iw44_encode + cleanup | Row-major plane read + ZIGZAG_INV scatter into L1 block (mirrors decoder); deletes 2 dup zigzag tables. Byte-identical. Round-6 swarm P5 |
| EPUB_PNG_COMPRESSION | 2026-07-03 | EPUB export | X | rejected on verification | Swarm P1 claimed −43.5% via png Fast, but Fast makes files **2.84× larger** (measured 271KB→770KB) — bad trade for a doc format. Default already balanced. Case study: verify swarm claims |
| LAZY_PAGE_CONSTRUCT | 2026-07-03 | container cold-open | **K** | **−48% from_bytes(520pp), −9% open+render-first**; mmap zero-copy | Bundled pages defer per-chunk copy: `ChunkStore::Lazy` holds shared backing (`Arc<dyn AsRef<[u8]>>`) + FORM range, materialises on first access. from_bytes/mmap funnel owned backing. Byte-identical (mmap parity test). Swarm P3 |
| PGO | 2026-07-03 | build profile (all paths) | **K** (opt-in) | **−15% cold render**; codec kernels flat/±1% | Profile-guided opt over fat-LTO. Big win on branch-heavy cold end-to-end render; neutral on LTO-inlined SIMD decode kernels (2 micro-benches +1–2%). Opt-in only (`make pgo`, corpus/host-specific profdata, can't ship to crates.io). `examples/pgo_train.rs` + `scripts/pgo.sh`. NB: the 2026-07-02 *manual* attempt (PGO_MANUAL) regressed encode +28%/+12% via an instrumented-build/layout mismatch — any PGO artifact work must use this round's recipe and watch encode benches. Round-5 #4 |
| INTEROP_PIXDIFF | 2026-07-03 | render quality (infra) | **K** (tool) | quality floor: mean <0.25/255 vs ddjvu | `examples/interop_pixdiff.rs` — per-pixel diff vs DjVuLibre. Palette/bilevel byte-identical; colour mean <0.25 (bilinear chroma #422). Validates round-5 changes pixel-faithful. Round-5 #14 |
| QUALITY_AA | 2026-07-03 | color/mask AA | D/F | deferred | Linear-light blend (#12) + mask upscale AA (#13) would diverge from the near-identical DjVuLibre reference; opt-in quality-mode territory, needs aesthetic judgement. Gate on scale>1. Round-5 #12/#13 |
| AVX2_IDWT | 2026-07-06 | IW44 IDWT (x86) | **X** (rejected) | activating existing AVX2 column-pass kernel **regresses** `iw44_to_rgb_colorbook` ~10-24% (two independent activation mechanisms agree) | Runtime-dispatched (`is_x86_feature_detected!`, `OnceLock`) the pre-existing but compile-time-dead `load8s_s1_avx2`/`store8s_s1_avx2` kernels; bit-exact (exhaustive equivalence test, CI `Test (stable)` on x86_64) but measurably slower, not faster — confirms round-47's flag-based finding with a real hand-written kernel. Reverted; closed with data. Round-5 #8, Round-47, Round-54 |
| ROI_IDWT | 2026-07-03 | render_region | X | low-value | Cold `decode_layers` cost is ZP-decode-bound (serial entropy stream, no spatial subset); only IDWT+convert is narrowable and it's cold-once. Warm region render already scoped. Round-5 #1 |
| DOC_SHARED_DICT_CACHE | 2026-07-03 | decode pipeline | **K** | **−37% multi-page shared-dict mask decode** | Decode shared JB2 dict once per document, not per page. `SharedDict{raw,OnceLock}` folds decode into the shared `Arc`. Byte-identical; scales with page:dict ratio. Fixtures existed (DjVu3Spec 71/5, czech 85/2, pathogenic 520/52). Round-5 #3 |
| SUB4_RGB_CACHE | 2026-07-03 | decode pipeline | **K** | **−4.9% color_downscale (sub=4 warm)** | Cache decoded BG44 RGB at sub=4 (from partial image); mirror of BG_CACHE/BG_CACHE_S2 for heavy-downscale/thumbnail. Byte-identical, ~2 MB/page. Round-5 open hypothesis #20 |
| BZZ_DEC_MTF | 2026-07-03 | BZZ decode | X | already covered | PS1 `copy_within` memmove already present at decode.rs:418; residual loop is a freq-sorted early-exit insert, not a plain shift. Round-5 #19 |
| IDWT_PAR_PLANE | 2026-07-03 | IW44 decode | D/F | cold-only, 3.26 ms reconstruct+convert baseline | Within-Y-plane wavelet parallelism. Deferred: cold-only (warm uses bg_rgb_s1), convert already parallel, needs per-thread scratch + per-scale barriers in normative SIMD loop, marginal on corpus. Design + fixture need recorded. Round-5 #2 |
| RENDER_REGION_SCOPE | 2026-07-03 | render_region | D | no change | `render_region` already composites only the region (offset + out_w×out_h); no full-page composite/crop. Residual cold-decode narrowing = ROI_IDWT (#1). Round-5 #1/#10 |

| IW44_MASKED_WAVELET | 2026-07-02 | IW44 encoder (size) | **CLOSED** (r21) | superseded by BG_DIFFUSE (r20) | Masked BG encoding (DjVuLibre c44 lever). **Resolved:** the low-risk first step (BG_DIFFUSE, r20) captured the whole win interop-safely; IW44_MASKED_TRANSFORM (r21) then measured the normative transform adds 0 on top (c44 -mask ≈ c44-on-diffused). No further masked work needed; residual gap is entropy coding, not masking. |
| PAR_SEGMENT | 2026-07-02 | segmentation (parallel) | **K** | `segment_page_color` **−39.7/−41.2%**; no multi-page nesting regression | Parallel BG-cell fill via `par_chunks_mut` row slices; byte-identical; `parallel` feature. The Sauvola *threshold* pass itself is still serial (→ issue #575). Sweep round 4 |
| PAR_TIFF | 2026-07-02 | TIFF export (parallel) | **K** | **−73%** | Parallel per-page image build, serial in-order IFD writes; SHA-256 byte-identity verified; new `export/tiff` bench. Sweep round 4 |
| PAR_EPUB | 2026-07-02 | EPUB export (parallel) | **K** | **−81%** | Parallel per-page artifact build, ordered writes (mirrors the PDF parallel exporter). CBZ was NOT covered by this sweep (→ issue #598). Sweep round 4 |
| PAR_CLUSTER | 2026-07-02 | JB2 shared-dict clustering (parallel) | **K** | `encode_djvm_bundle_jb2` **−58%**; layered −17…22% | Parallel per-page `extract_ccs`. Sweep round 4 |
| PGO_MANUAL | 2026-07-02 | build profile (PGO over LTO_FAT, 1st attempt) | X | `jb2_encode_dict` **+28%**, `iw44_encode_color` **+12%**, decode ≈0 | Manual PGO with the profile gathered from a **non-LTO cu=16 instrumented build** applied to a fat-LTO binary — layout mismatch not disambiguated from a real PGO-vs-LTO conflict. Superseded by round-5 PGO (Kept opt-in, −15% cold render, `scripts/pgo.sh`). Encode benches are the canary for any PGO-artifact work (#586) |
| JB2_SINGLETON_PRUNE | 2026-07-02 | JB2 encoder (dict membership) | **R** | **+0.070%** (+1,555 B / 120 pages; all 7 docs grew) | Rec-3 for exact-unique glyphs loses: adaptive index contexts already charge ≈0 for unused range, while a third `record_type` value raises that context's entropy. Its closing verdict — "the gap lives in glyph *matching*" — was then executed by SAME_SIZE_REC6 / LOSSY_B* (rounds 17–19) |
| PARTIAL_DECODE_SCALE | 2026-07-02 | IW44 decode (chunk budget @ sub=2) | D | drop-last-chunk @sub2: text lossless but ≈0 saving; photo −26…−41% decode at 43.2/30.6 dB | sub≥4 already uses `bg44_partial` (1 chunk); sub=2 decodes all chunks. No default change (30.6 dB borderline + interop divergence); opt-in "draft render" mode deferred as a feature PR. Lever considered closed |
| MIMALLOC | 2026-07-02 | memory (global allocator) | X | parallel ≈−1% (noise); `parse_multipage_520p` **+8.6%**, cold IW44 decode **+9.7%** | Interleaved same-session A/B (thermal-safe method). macOS libmalloc already fits the workload; a library must not pin `#[global_allocator]` anyway |
| PS4 | 2026-07-02 | JB2 encoder (`extract_ccs`) | **K** | `jb2_encode_dict` **−25.8%** | Byte-unpack in dict-encoder CC extraction (same pattern as PS2). Perf swarm |
| PS5 | 2026-07-02 | segmentation | **K** | `segment_page_color` **−14.0%** | `segment_page` row-slice mask + block-mean scan. Perf swarm |
| PS6 | 2026-07-02 | FGbz encoder | **K** | `encode_color_page_quality` **−2.2%** | `foreground_fgbz` row-slice blit-colour averaging. Perf swarm |
| PS-R2 | 2026-07-02 | IW44 decode | **R** | no consistent win | Register-hoist `newly_active_coefficient_decoding_pass` — reverted. Perf swarm |
| PS-R3 | 2026-07-02 | IW44 encode (RGB→YCbCr) | **R** | no consistent win | Row-slice colour conversion — reverted; chroma_half NEON upsample also assessed not-worth (carte-only path). Perf swarm |
| BENCH_0702 | 2026-07-02 | bench infra | D | — | Sweep bench workloads added (incl. `encode_color_page_quality`) |
| PS1 | 2026-07-01 | BZZ decode (MTF) | **K** | `bzz_decode` **−2.2%** | `copy_within` memmove + drop dead per-element `.ok_or()?` branches; byte-identical. (Round-6's BZZ_DEC_MTF "already covered" refers to this.) Perf swarm |
| PS2 | 2026-07-01 | JB2 encoder (bitmap unpack) | **K** | `jb2_encode` **−20.2%**, multitile **−25.9%** | Byte→8px unpack replaces per-pixel non-inlined `bm.get()` (integer divide per pixel). Biggest swarm encode win. Perf swarm |
| PS3 | 2026-07-01 | Lanczos (horizontal pass) | **K** | `render_scaled_0.5x/lanczos3` **−69.1%** | Per-column kernel weights precomputed once (horizontal analogue of #448); bit-identical. Perf swarm |
| PS-R1 | 2026-07-01 | area-avg compositor | **R** | no consistent win | FG44 row-invariant hoist (cf. dead-end §1). Perf swarm |
| JB2_DICT_HASH_DEDUP | 2026-07-01 | JB2 encoder (exact dedup) | **K** | **~2–3%** (36.6→35.8 / 42.8→41.9 / 27.2→26.4 ms) | FNV `symbol_hash` bucket + verify replaces the per-CC `Vec<u8>` key clone; byte-identical. Distinct from CLUSTER_BUCKET_HASH_DEDUP (round-15, clustering pass) |
| ENCODER_INTEROP_FIX | 2026-06-30 | encoder ↔ DjVuLibre (correctness) | **Fixed** | colour corpus in ddjvu: 0/6 → **6/6**; net size **+0.3%** after `crcbdelay=10` follow-up (−10…18% BG44) | Two encoder bugs invisible to our own round-trips: (1) IW44 major byte 0 → 0x01/0x81 (#462, `IWCODEC_MAJOR`); (2) **the chroma_half saga** — our `chroma_half=true` default emitted half-resolution chroma, but DjVuLibre's `IWPixmap::decode_chunk` **always builds/reads full-resolution chroma** (root-caused from fetched source after black-box testing was exhausted; real c44 output: `crcbdelay=10`, never `crcb_half`) → default flipped to full-res. Cite before proposing any half-res-chroma work (closed #587); decode-side suspicion for carte noise → #561 |
| LTO_FAT | 2026-07-02 | build profile (all paths) | **K** | **−65% jb2_encode_dict, −7% bzz/segment, −2…3% iw44** | `lto="fat"` + `codegen-units=1` release/bench profile (none existed before). Cross-crate ZP inlining; render flat. Cost: ~2× build time |
| PAR_ENCODE | 2026-07-02 | encoder (multi-page) | **K** | **−35…43% layered, −39% bundle_jb2** | Parallel per-page encoding via rayon in both DJVM bundlers (`parallel` feature); byte-identical, seq fallback. Decode was parallel (PAR_DEC) but encode wasn't — largest untouched lever |
| ENC_SPEED_DIAG | 2026-06-26 | encoder (speed) | D | IW44 1.46× faster than c44; JB2 ~par with cjb2 | Head-to-head on identical inputs. Encode speed competitive — no hot-spot; axis healthy, not a gap |
| JB2_PAGE_SYM_CAP | 2026-06-25 | JB2 decode (robustness) | **K** | fuzz_jb2 timeout fixed; per-page decode bounded 256→16 MP | 409 B input did 23 K refinement records = 47.7 MP (~626 ms→10 s ASAN). Split cap: 16 MP page / 256 MP dict (32→16 for fuzz margin). Regression test + seed corpus |
| IW44_SWARM_REST | 2026-06-25 | IW44 encoder (size) | R/X | dead-end / interop-break | #8 ycbcr-round (PSNR worse), #6/7 early-term (chunks not null), #3/4/5/10/12 (change normative tables/ctx → break DjVuLibre interop), #2 (quality knob) |
| IW44_ACT_THRESH (IW44-1) | 2026-06-25 | IW44 encoder (size) | **K** | **−9.1% BG44, PSNR-identical** | Activation prediction s/2→11s/16 matches real gate; removes wasted NEW bits. Encoder-only, interop-safe. Gap 14.3%→3.9% |
| ENC_SIZE_DIAG | 2026-06-25 | encoder (size) | D | mask=parity, IW44 BG44=1.143× | Chunk breakdown: BG44/FG44 dominate colour/photo docs (94–99.9%); our IW44 is ~14% larger → next size lever |
| LAYERED_SHARED_DJBZ (#452) | 2026-06-24 | JB2 encoder (size) | **K** | **−34% (spec) / −5.5% (watchmaker)** | Shared Djbz for layered multi-page encode; closes the real DjVuLibre size gap (per-page dict dup). Round-trip verified |
| CHROMA_BILINEAR (#422) | 2026-06-24 | IW44 YCbCr | **Superseded** | n/a | Its `chroma_half` premise was invalid for the only fixture (`carte.djvu`): round-57 #561 established that v1.2 Cb/Cr are full-resolution even when `delay_byte`'s high bit is clear. |
| REFROW_REG (#445) | 2026-06-24 | JB2 decode | R | no benefit | Rolling m_r2 register; col_shift unbounded so guard can't drop; safe version just relocates read |
| IDWT_S2_NEON (#442) | 2026-06-24 | IW44 IDWT NEON | X | incorrect premise | Reuse s1_row for s==2 fails correctness: s==2 ⟹ sd==1 (stride-2 cols), s1_row assumes stride-1 |
| IDWT_SPLAT (#441) | 2026-06-24 | IW44 IDWT NEON | R | unmeasurable | Hoist vdupq_n_s32 splat; movi is free in ALU-bound loop, no benefit. C16/C8 pattern doesn't transfer |
| F2_GAMMA (#443) | 2026-06-24 | color 1:1 bilinear | **K** | narrow (non-identity gamma) | F2 all-bg fast path for non-identity gamma (LUT pass); free-on-miss, byte-identical. Identity path unchanged |
| PDF_STREAM (#449) | 2026-06-24 | PDF export | **K** | peak RSS O(pages)→O(1) bodies | Stream render→emit→drop in sequential path; byte-identical, strictly ≤ peak. Unmeasurable on corpus (no many-large-body doc) |
| LANCZOS_HOIST (#448) | 2026-06-24 | Lanczos resample | **K** | **−22.5% Lanczos** | Hoist sin weights + row-major accumulate in vertical pass; bit-identical |
| ROTATE_TILE (#447) | 2026-06-24 | rotate_pixmap | **K** | ~2–6% rotated (transpose much faster) | 32×32 tiled transpose for Cw90/Ccw90; cache-local vs strided. Byte-identical |
| CLUSTER_DEDUP (#446) | 2026-06-24 | JB2 encoder | **K** | O(P²)→O(P) | pages_seen.contains→last() under monotonic page order; strictly ≤ cost, helps large multi-page encodes |
| FGPAL_CACHE (#444) | 2026-06-24 | decode pipeline | R | +1% regression | Cache parsed FGbz palette; navm_fgbz chunk is 21 bytes (trivial parse) → cache overhead > saving |
| PAR_DEC (#440) | 2026-06-24 | decode pipeline | **K** | **−22% cold render** | rayon::join BG/FG layer decode (parallel feature); FG overlaps BG ZP phase. Warm free, cold first-open faster |
| COLOR_AA (#439) | 2026-06-24 | color area-avg | **K** | quality (68 vs 28 colors) | Proportional fg/bg blend via mask_box_coverage; AA color downscale, removes blocky halos. mask_box_any deleted |
| AREAAVG_ALLBG (#438) | 2026-06-24 | color area-avg | **K** | ~skip mask_box_any (50–71% fire) | All-bg band short-circuit skips footprint scan; byte-identical, #428 analog for color downscale |
| EXACT_SLICE (#437) | 2026-06-24 | color 1:1 bilinear | R | unmeasurable | Exact-length row slices for bounds-check elision; unwrap_or(&[]) defeats fg length-tracking; machine too hot to verify |
| BSERIES_MASKEXP (#436) | 2026-06-24 | B-series upscale | R | unmeasurable + overhead | MASK_EXPAND in B-series (G1b retry on upscale); expansion overhead not worth it on resampling-bound path |
| BSERIES_ALLBG (#435) | 2026-06-24 | B-series bilinear | **K** | ~3–8% (52.6% fire) | All-bg row short-circuit skips is_fg; byte-identical, F2 analog. Benchmark noisy, firing-rate evidence |
| FG_FX_ACCUM (#434) | 2026-06-24 | B-series bilinear | R | +13–27% regression | Q48 accumulator for fg_fx; advance is unconditional (100%) but fg multiply was conditional (~20%). Inverse of B1 |
| P2_REGION (#433) | 2026-06-24 | bilevel 1:1 | **K** | compositor faster, e2e within noise | Generalize P2 to byte-aligned offset_x (render_region); safe ext, zero regression on offset_x==0 |
| MASK_ANY_BYTE (#432) | 2026-06-24 | color area-avg | R | +5.1% regression | Byte-scan mask_box_any; early-exit + small footprint → setup overhead > saving. POPCNT doesn't transfer |
| TIFF_LUT (#431) | 2026-06-24 | TIFF export | **K** | **~18% bilevel export** | LUT byte-expansion in extract_bilevel_pixels (Gray8 analog of P2 table) |
| TIFF_DEFLATE (#430) | 2026-06-24 | TIFF export | **K** | **~149× size** | Deflate-compressed bilevel TIFF (1-bit not supported by tiff 0.9; Deflate beats it) |
| CROP_BYTECOPY (#429) | 2026-06-24 | JB2 direct encoder | **K** | **~14–15% multitile** | Byte-aligned crop_bitmap row-copy; direct encode_jb2 path only (non-shipping) |
| I3_DOWNSCALE (#428) | 2026-06-24 | bilevel downscale | **K** | **~6.5% (94.4% fire)** | Blank-band fill(255) in AA downscale path; benchmark thermal-noisy, evidence from fire rate + cost model |
| MASK_IDX_CACHE (#427) | 2026-06-24 | decode pipeline | **K** | **~5% palette** | Cache indexed JB2 mask+blit-map; avoids 33.6 MB re-alloc + JB2 re-decode per render |
| BG_CACHE_S2 (#426) | 2026-06-24 | decode pipeline | **K** | **−7–9% downscale** | Cache decoded BG44 RGB at sub=2; mirror of BG_CACHE for 150 DPI |
| C2c | 2026-06-24 | area-avg compositor | R | 0% | LLVM LICM already hoists fg_fy. Related: C2b |
| C2b | 2026-06-24 | B-series bilinear | R | 0% | LLVM LICM already hoists fg_fy. See dead-end §1 |
| P2 | 2026-06-24 | bilevel 1:1 | **K** | **+18–24% bilevel** | BILEVEL_RGBA 256×32B table; NEON `ldp q0,q1/stp q0,q1`. Replaces E1 |
| Q1 | 2026-06-24 | bilevel 1:1 | R | 0% | Clamp removal; LLVM still scalar. Superseded by P2 |
| G1b | 2026-06-24 | B-series bilinear | R | −10% regression | MASK_EXPAND in downscale path; overhead > saving at 72 DPI |
| I3 | 2026-06-24 | bilevel 1:1 | **K** | **+2.3% bilevel** | All-zero mask row → `row_buf.fill(255)`. Fires before P2 |
| G2 | 2026-06-24 | color 1:1 bilinear | R | 0% | bg-fill-then-fg-fixup; LLVM already pipelines loads. See dead-end §2 |
| G1 | 2026-06-24 | color 1:1 bilinear | **K** | **+25% color** | MASK_EXPAND to 4096B stack buf; 1 byte-load replaces 7-op bit-extract |
| F2 | 2026-06-24 | color 1:1 bilinear | **K** | **+3.4% color** | All-bg mask row → bulk `copy_from_slice`. Mirrors I3 |
| E1 | 2026-06-24 | bilevel 1:1 | R | 0% | MASK_EXPAND+fill in bilevel path; LLVM auto-vec already optimal |
| C3 | 2026-06-24 | color 1:1 bilinear | **K** | ~0% (cleanup) | Mask-row hoist; LLVM did LICM; kept for code clarity |
| POPCNT | 2026-06-23 | bilevel downscale | **K** | **−24% @72DPI** | Byte-level popcount in `mask_box_coverage`; replaces per-pixel bits |
| AA | 2026-06-23 | bilevel downscale | **K** | quality ↑ | Anti-aliased bilevel via `mask_box_coverage`; was binary `mask_box_any` |
| C2 | 2026-06-23 | color 1:1 bilinear | **K** | −1.4% color | FG44 y-row + bg-row hoist in general 1:1 path |
| BILINEAR | 2026-06-22 | color 1:1 bilinear | **K** | ~0% (+0.4% noise) | quality: `sample_nearest` → `sample_bilinear` for FG44 |
| ZEROED | 2026-06-22 | output allocation | X | −1% seq / **+62% par** | `Pixmap::zeroed` triggers VM page-fault storm in parallel path |
| COW_FG | 2026-06-21 | decode pipeline | **K** | −1.7% seq / −6.1% par | Cow for FG44+Mask; avoids 4.75 MB clone each warm render |
| FILL_OVR | 2026-06-21 | color 1:1 bilinear | X | +3.3% regression | Fill+overlay 2-pass; doubles output bandwidth vs 1-pass |
| COW_BG | 2026-06-21 | decode pipeline | **K** | −1.2% seq / −7.2% par | Cow for BG44; avoids 33.6 MB clone each warm render |
| ALPHA_INL | 2026-06-21 | color 1:1 bilinear | **K** | −2.3% seq / **−12.6% par** | Inline alpha=255 per pixel; removes 33.6 MB sequential post-pass |
| BG_CACHE | 2026-06-21 | decode pipeline | **K** | −5.1% color | Cache decoded BG44 RGB Pixmap; avoids 2.8 ms IDWT+YCbCr per call |
| BILEVEL_NEON | 2026-06-21 | bilevel 1:1 | X | +1.8% regression | Manual NEON `vst4_u8`; LLVM auto-vec already optimal. See dead-end §3 |
| COLOR_NEON | 2026-06-21 | color 1:1 bilinear | X | +0.9–1.3% regression | Manual NEON for A2 path; inner loop not bottleneck |
| SIMD_AREA | 2026-06-21 | area-avg downscale | X | +19.9% regression | NEON/SSSE3 accumulate; 2×2 footprint too small for SIMD threshold |
| PARALLEL | 2026-06-18 | all paths | **K** | **3.8× parallel** | `par_chunks_exact_mut` in `composite_into`; opt-in via `parallel` feature |
| IW44_PAR | 2026-06-21 | IW44 decode | **K** | **2.2× IDWT** | Parallel Y/Cb/Cr IDWT via `rayon::join`; opt-in |
| AREA_FIX | 2026-06-18 | area-avg downscale | **K** | **−52% colorbook** | Fix off-by-one exclusive bound; was 3×3 at 2×, now correct 2×2 |
| JB2_CROSS6 | 2026-06-15 | JB2 encoder | R | +4.37% size | Cross-size record-6; geometric misalign makes context mispredict |
| IW44_DIAG | 2026-06-15 | IW44 encoder | D | — | Loss localized to fine-band quantization; transform is bit-exact |
| RENDER_DIRECT | 2026-05-16 | render pipeline | **K** | **−18–20%** | `render_pixmap` direct → `render_into`; removes row-copy adapter |
| TIFF_STREAM | 2026-05-16 | TIFF export | **K** | −116 MB peak RSS | `render_streaming` feeds TIFF rows; no full RGBA pixmap |
| ADAPTIVE_SEG | 2026-05-16 | encoder segmentation | **K** | quality ↑ | Sauvola binarization + BG inpainting; opt-in via `SegmentOptions` |
| DJVM_MULTI | 2026-05-16 | encoder | **K** | — | Layered multi-page DJVM via CLI `--quality` |
| JB2_QUALITY | 2026-05-17 | JB2 encoder | F | dict=1.356× orig | Round-trip ok; byte cost remains too high vs original |
| WASM_SIMD | 2026-05-17 | wasm | D | −7% render | Harness: scalar vs simd128; simd128 confirmed win from existing IW44 code |
| ROW_SCRATCH | 2026-05-17 | render pipeline | X | inconsistent | Noisy A/B; rejected as production heuristic |
| COMP_BASELINES | 2026-05-17 | infrastructure | D | — | Added `render_compositor_only` bench group |

---

## Render Path Map

```
render_pixmap (RENDER_DIRECT: kept)
└── composite_into / composite_rows
    ├── [parallel] composite_rows via par_chunks_exact_mut   (PARALLEL: kept)
    ├── composite_rows_bilevel_one                            ← bilevel 1:1 path
    │   ├── I3: all-zero row → fill(255)                     KEPT
    │   ├── P2: BILEVEL_RGBA table, offset_x==0             KEPT  ← hot path
    │   ├── mask_bilinear_coverage (opt-in mask_aa, upscale) KEPT (D_AA_ZOOM)
    │   └── scalar fallback (bit-extract per pixel)
    ├── composite_rows_bilinear_one
    │   ├── [1:1] general fast path                          ← color 1:1 path
    │   │   ├── F2: all-bg mask row → copy_from_slice        KEPT
    │   │   ├── G1: MASK_EXPAND pre-expand to stack buf      KEPT  ← hot path
    │   │   └── per-pixel bilinear blend
    │   └── [B-series] non-1:1 path                         ← color downscale/upscale
    │       ├── sample_bilinear(fg, fg_fx, fg_fy)            (C2b: LLVM already LICM)
    │       ├── mask_box_coverage / POPCNT                   KEPT (downscale)
    │       └── mask_bilinear_coverage (opt-in mask_aa)       KEPT (D_AA_ZOOM, upscale)
    └── composite_rows_area_avg_one                          ← area-avg downscale
        ├── AREA_FIX: correct 2×2 exclusive bound            KEPT
        └── sample_area_avg_bounds                           (SIMD: rejected)

Decode pipeline:
  BG44 → RGB cache (BG_CACHE) → Cow borrow (COW_BG) → compositor
  FG44 → Cow borrow (COW_FG) → compositor
  JB2  → Cow borrow (COW_FG) → compositor
  IW44 IDWT: parallel Y/Cb/Cr (IW44_PAR)
  Output alpha: inlined per-pixel (ALPHA_INL)
```

---

## Current Baselines (cool state, M1 Max, aarch64, Rust 1.88)

| Benchmark | Time | Path exercised |
|-----------|------|----------------|
| `bilevel_native_cached` | ~37 ms | bilevel 1:1 (P2 + I3 active) |
| `color_native_cached` | ~47 ms | color 1:1 (G1 + F2 active) |
| `color_downscale_mixed_cached` | ~15–20 ms | B-series bilinear (watchmaker @150/300) |
| `render_corpus_bilevel_dpi/72` | ~7 ms | bilevel downscale (POPCNT) |
| `render_corpus_bilevel_dpi/150` | ~23 ms | bilevel downscale (POPCNT) |
| `render_colorbook` | ~3.5 ms | area-avg downscale (AREA_FIX) |
| `encode_color_page_quality_bgheavy` | ~40 ms | single-page colour encode on the BG-heavy composited fixture (seg 21 / jb2 7.8 / iw44 1.8 ms) — the round-56 bench for colour-encode micro-parallelism |

Thermal methodology: use intra-session ratio `target / control` to cancel M1 Max throttling.
Control for bilevel experiments: `color_native_cached`. Control for color: `bilevel_native_cached`.

---

## Known Dead Ends (do not retry without new evidence)

1. **LLVM LICM** covers all y-row hoists in B-series / area-avg paths (C2b, C2c). Confirmed by intra-session ratio showing zero consistent trend. Any explicit hoisting adds Option wrapping that may inhibit other opts.
2. **LLVM out-of-order pipelining** covers independent bg_row + g1_mask loads in the G1 inner loop (G2). A second pass over g1_mask to find fg pixels adds sequential dependency that prevents the same pipelining.
3. **LLVM auto-vectorization** of the bilevel scalar loop is already optimal on aarch64-apple-darwin (BILEVEL_NEON, E1, Q1). Manual NEON / `#[target_feature]` on stable Rust cannot use `#[inline(always)]` (issue #145574).
4. **SIMD for small footprints** (SIMD_AREA): at 2×2 downscale, each `sample_area_avg_bounds` row has 2 pixels = 8 bytes, below any SIMD threshold. Falls through to scalar tail every call.
5. **`Pixmap::zeroed`** (lazy VM pages): causes page-fault storm in parallel compositor on macOS; 21–62% regression in parallel path.

---

## Open Hypotheses (tracked as GitHub issues)

Generated 2026-06-24 by the `perf-experiment-swarm` workflow (14 discovery agents →
adversarial validation → synthesis). Each survived a skeptical validator that checked
against all 37 prior experiments and the 5 dead-ends. Pick one, run the experiment,
record the result in `PERF_EXPERIMENTS.md`, close the issue.

**P1 (highest leverage):**
- ~~#426 — cache decoded BG44 RGB Pixmap at subsample=2~~ **DONE (Kept, −7–9%)** → BG_CACHE_S2
- ~~#427 — cache decoded JB2 indexed mask in PageLayers~~ **DONE (Kept, ~5%)** → MASK_IDX_CACHE
- ~~#428 — all-white band fast path in bilevel downscale compositor~~ **DONE (Kept, ~6.5%)** → I3_DOWNSCALE
- ~~#429 — crop_bitmap aligned byte-copy fast path in JB2 encoder~~ **DONE (Kept, ~14–15% multitile)** → CROP_BYTECOPY
- ~~#430 — 1-bit TIFF output for bilevel pages~~ **DONE (Kept, ~149× via Deflate; 1-bit unsupported by tiff 0.9)** → TIFF_DEFLATE

**P2:**
- ~~#431 LUT byte-expansion in bilevel TIFF~~ **DONE (~18%)** → TIFF_LUT · ~~#432 byte-level early-exit in `mask_box_any`~~ **REVERTED (+5% regression)** → MASK_ANY_BYTE
- ~~#433 extend P2 to byte-aligned offset_x~~ **DONE (Kept, safe ext)** → P2_REGION · ~~#434 B-series fg_fx Q48 accumulator~~ **REVERTED (+13–27% regression)** → FG_FX_ACCUM
- ~~#435 B-series all-bg row fast path~~ **DONE (Kept, 52.6% fire)** → BSERIES_ALLBG · ~~#436 MASK_EXPAND in B-series upscale~~ **REVERTED (overhead, unmeasurable)** → BSERIES_MASKEXP
- ~~#437 exact-length FG44/BG44 row slices~~ **REVERTED (unmeasurable, mechanism defeated)** → EXACT_SLICE · ~~#438 area-avg all-bg row fast path~~ **DONE (Kept, 50–71% fire)** → AREAAVG_ALLBG
- ~~#439 proportional fg/bg blend in color area-avg~~ **DONE (Kept, quality)** → COLOR_AA · ~~#440 PAR-DEC parallel layer decode~~ **DONE (Kept, −22% cold)** → PAR_DEC

**P3:**
- ~~#441 hoist NEON splat consts in IDWT~~ **REVERTED (movi is free in the ALU-bound loop, no benefit)** → IDWT_SPLAT · ~~#442 NEON s=2 row pass in IW44 IDWT~~ **REJECTED (incorrect premise: s==2 implies sd==1)** → IDWT_S2_NEON
- ~~#443 F2 fast path for non-identity gamma~~ **DONE (Kept, safe ext)** → F2_GAMMA · ~~#444 cache parsed FGbz palette~~ **REVERTED (+1%, 21-byte chunk)** → FGPAL_CACHE
- ~~#445 rolling 1-bit register in decode_ref_row~~ **REVERTED (col_shift unbounded, no benefit)** → REFROW_REG · ~~#446 O(1) dedup in cluster_shared_symbols~~ **DONE (Kept, O(P²)→O(P))** → CLUSTER_DEDUP
- ~~#447 cache-tiled transpose in rotate_pixmap~~ **DONE (Kept, ~2–6%)** → ROTATE_TILE · ~~#448 Lanczos3 vertical-pass weight precompute~~ **DONE (Kept, −22.5%)** → LANCZOS_HOIST
- ~~#449 PDF streaming render-and-emit~~ **DONE (Kept, strictly ≤ peak)** → PDF_STREAM

**Pre-existing (not from swarm):** ~~#422 bilinear chroma upsampling~~ **DONE (historical;
superseded by #561's verified full-chroma v1.2 header interpretation)** → CHROMA_BILINEAR ·
~~#423 Lanczos-3 resampling option~~ **DONE (shipped; D2 verdict: decisively better than
bilinear, SSIM 0.99 vs 0.86–0.96)** → QUALITY_HARNESS_D1

**From prior analysis:** ~~JB2 byte-cost estimator before cross-size emit (#301; JB2_CROSS6 was
+4% size — need cost model first)~~ **CLOSED: #301 built the estimator (2026-05-17), but the #322
real emitter still lost +4.37% — cross-size rec-6 is dead in lossless and lossy form; the lever
that won is same-size rec-6 (SAME_SIZE_REC6 / LOSSY_B_SHIP)**. ~~IW44 quantization/slice-budget increase
for fine bands (IW44_DIAG)~~ **CLOSED by IW44_SLICE_RD (round 18):** the RD curve shows
`total_slices=100` is at the knee and avg_abs=8.72 is the transform's quantization floor, not
slice starvation — more slices explode size for ≤0.003 SSIM.

### 2026-07-10 — experiment-candidate backlog (issues #557–#603)

A code+log audit generated 47 experiment issues (30 + 17). After cross-checking against
the **full log** (not just this index), 43 remain open — highlights: P1 #561 (carte
chroma noise — see ENCODER_INTEROP_FIX for the sharpened header-interpretation
hypothesis), #562 (text-vs-photo segmentation classifier); P2 #557 (iai-callgrind
instruction-count channel), #559/#563 (PDF stencil colour / true-MRC), #565 (streaming
#567 (encoder fuzz gaps), #588 (wasm lazy Range open), #589 (resource-ceiling audit).
Closed same-day as already answered by entries this index was missing: **#587**
(chroma_half interop → ENCODER_INTEROP_FIX), **#574** (within-plane forward DWT →
PAR_PAGE_LAYERS round-56 stage split), **#560** (rotation fusing → round-8 C2 verdict),
**#564** (near-size lossy matching → LOSSY_B1 "not re-attempted" + LOSSY_B0
saturation). That failure mode is why the addendum below and the maintenance rule at
the top exist.

**2026-07-10 rerank (same-day follow-up).** #88 reopened as **P1**: the `bench.yml` PR gate is
silently broken end-to-end (baseline extracted to `baseline/` but compared at
`baseline/target/criterion`; bench steps `continue-on-error`; a real regression kills the compare
step under bash `-e` before `regression_exit` is written, so "Fail on regression" is skipped;
empty current results return 0 — found in the wild by ZP_U64, round 55). Priority moves:
#559 P2→P1 (PDF flattens coloured foreground to black — silent fidelity loss), #562 P1→P2
(blocked on mixed-layout fixtures), #558 P3→P2 (now gates #562/#569–#571/#591–#593/#603),
waits on #557, which itself waits on the #88 repair). Scope split: #599 owns EPUB/CBZ PNG
colour-type payloads (RGB + Gray8), #580 keeps adaptive PNG/JPEG only. New experiment issue:
#612 — build the FGbz palette from encoder blit metadata instead of `foreground_fgbz`'s
decode-after-encode round-trip. Stale rows in "Open Hypotheses" above (#441, #422/#423, #301)
struck through the same day.

---

## Addendum (2026-07-10 audit): previously unindexed entries

Status-only pointers; numbers live in the full log entries.

### June 09–19 compositor / coder sweep

| Entry (log name) | Date | Status | One-liner |
|---|---|---|---|
| IW44 slice-loop early-exit (`zp.is_exhausted()`) | 2026-06-09 | **R** | early-exit reverted |
| JB2 post-EOF guard (`pos` overshoot) | 2026-06-16 | **K** | robustness: synthetic-byte overshoot instead of `is_exhausted()` |
| BILINEAR_1_1_NEAREST | 2026-06-18 | **K** | `sample_nearest` at exact 1:1 scale |
| DECODE_SCALE_ROUND | 2026-06-18 | **K** | `.round()` in `best_iw44_subsample` |
| BILEVEL_EXPAND_LUT | 2026-06-18 | **R** | early LUT attempt; superseded by P2 (2026-06-24, kept) |
| A2 (MASK_EXPAND LUT + branchless blend, tight 1:1) | 2026-06-18 | **K** | precursor of G1 |
| B1 (bg_fy hoist + incremental bg_fx accumulator) | 2026-06-18 | **K** | general bilinear path |
| B1b (MASK_EXPAND specialization for cable) | 2026-06-18 | **R** | — |
| B2 (precompute BG row slices) | 2026-06-18 | **K** | — |
| B2b (mask row slice hoist) | 2026-06-18 | **K** | — |
| B2c (fx multiply → running accumulator) | 2026-06-18 | **R** | — |
| B2d (two bounds checks per row) | 2026-06-18 | **R** | — |
| LERP_NO_CLAMP | 2026-06-18 | **K** | remove `min(255)` from bilinear lerp |
| C1_SIMD (`wide::u32x4` lerp) | 2026-06-18 | **R** | SIMD lerp loses (cf. dead-ends §3/§4) |
| D1 (gamma identity fast path) | 2026-06-18 | **K** | skip LUT reads when gamma = identity |
| E1 (no-mask bulk memcpy, gamma-identity 1:1) | 2026-06-18 | **K** | distinct from the 06-24 "E1" (reverted MASK_EXPAND batch) |
| E2 (specialized no-mask bilinear loop) | 2026-06-18 | **R** | — |
| F3 (4-byte pixel read in `bilinear_from_rows`) | 2026-06-18 | **K** | — |
| F3b (4-byte BG read in A2 loop) | 2026-06-18 | **K** | — |
| G1 4-byte RGBA write in A2 | 2026-06-18 | **R** | distinct from the 06-24 "G1" (kept MASK_EXPAND) |
| A3 (mb==0 bulk-copy in A2) | 2026-06-18 | **R** | — |
| AA1 (u64 bg_fx accumulator, area-avg) | 2026-06-18 | **R** | — |
| H1 (4-weight bilinear lerp precompute) | 2026-06-19 | **K** | — |
| H2 (slice-trim to exact stride) | 2026-06-19 | **R** | — |
| H3 (FG44 x-coordinate accumulator) | 2026-06-19 | **R** | — |
| #416 H1 (horizontal coord table) | 2026-06-19 | **R** | — |
| #416 H2 (black-mask identity dispatch) | 2026-06-19 | **R** | — |
| #416 H3 (byte-run mask traversal) | 2026-06-19 | **R** | — |
| #416 H4 (precompute area-avg BG x/y bounds) | 2026-06-19 | **K** | — |
| #416 H5 (integral-image BG downscale sampling) | 2026-06-19 | **R** | — |
| #416 H6 (fuse alpha write into area-avg rows) | 2026-06-19 | **K** | — |

### Pre-index era (2026-04-18 … 2026-05-17)

| Entry | Date | Status | One-liner |
|---|---|---|---|
| #185 bit-pack `Jbm` to 1 bpp | 2026-04-18 | **K** | JB2 memory/layout foundation |
| #184 IW44 `column_pass` SIMD at s=2 | 2026-04-18 | **R** | cf. later IDWT_S2_NEON rejection (same premise) |
| #194 Ph2 multi-page shared Djbz w/ Hamming clustering | 2026-04-28 | **R** default | kept as tunable knob; see #258 |
| #194 Ph2.5 per-CC accounting harness | 2026-04-28 | **K** (instr) | — |
| #224 Ph4 opt-in lossy rec-7 substitution | 2026-04-28 | **K** | became `lossy_threshold` (see LOSSY_B0/B_SHIP) |
| #190 Ph2 wasm simd128 inverse wavelet | 2026-04-29 | **K** | — |
| #225 Ph1 internal row-streaming render | 2026-04-29 | **K** | — |
| #225 Ph2 public `render_streaming` API | 2026-04-30 | **K** | — |
| #229 PR1 extract `djvu-zp` crate | 2026-04-30 | **K** | — |
| #189 Ph3 AVX2 `prelim_flags_*` ports | 2026-04-30 | **K** | — |
| #222 PR1 `DjVuDocumentMut` + chunk replacement | 2026-04-30 | **K** | — |
| #222 PR2 high-level setters | 2026-05-01 | **K** | — |
| #233 async lazy first-page probe | 2026-05-04 | **K** | → `LazyDocument` lineage |
| #189 x86-64-v3 AVX2 validation | 2026-05-04 | **K** partial/F | — |
| #258 shared-Djbz Hamming clustering | 2026-05-04 | **X** | produced invalid output; cited in `encode.rs` comments |
| #283 cross-size refinement probe | 2026-05-12 | **K** (instr) | → #301/#322 lineage, all dead ends |
| #278 PR1 single-page Quality/Archival FGbz profiles | 2026-05-12 | **K** | — |
| #289 per-blit FGbz indices | 2026-05-12 | **K** | — |
| #292 cross-architecture benchmark matrix | 2026-05-17 | **K** | — |
| #295 JB2 corpus round-trip + size baseline | 2026-05-17 | F | refreshed later (issue #295 closed) |
| #298 PDF export memory + parallel baseline | 2026-05-17 | F | → PDF_STREAM/#449, PDF parallel |
| #299 PDF color row streaming | 2026-05-17 | **K** | no full RGBA/RGB staging in PDF |
| #300 IW44 low-PSNR diagnosis (conquete_paix) | 2026-05-17 | F | → #320: loss = finest-band quantization schedule |
| #301 JB2 cross-size byte-cost estimator | 2026-05-17 | F | → #322 emitter: loses bytes, stays behind probe flag |
| #307 x86 AVX2 row-pass feasibility spike | 2026-05-17 | **X** | cf. AVX2_IDWT rejections (round 5/47/54) |
| #308 aarch64 NEON validation | 2026-05-17 | **K** partial | — |
| Post-roadmap render baseline correction | 2026-05-17 | **K** | — |
| Post-roadmap full benchmark refresh | 2026-05-17 | F | — |
